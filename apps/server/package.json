{
  "name": "@auto-mb/server",
  "//version": "The OpenAPI document publishes this exact string (src/api-version.ts). Minor tracks the highest delivered milestone in docs/ROADMAP.md; major stays 0 while production gates remain open.",
  "version": "0.11.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "start": "tsx src/main.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@auto-mb/contracts": "workspace:*",
    "@auto-mb/db": "workspace:*",
    "@auto-mb/documents": "workspace:*",
    "@auto-mb/loa-parser": "workspace:*",
    "@fastify/swagger": "9.8.1",
    "@fastify/type-provider-typebox": "5.2.0",
    "@sinclair/typebox": "0.34.52",
    "better-auth": "1.6.26",
    "fastify": "5.10.0",
    "nodemailer": "9.0.4",
    "pg": "8.22.0",
    "qrcode": "1.5.4"
  },
  "//devDependencies": "@fastify/swagger-ui sits here, not in dependencies: @fastify/swagger stays a runtime dependency because it generates the OpenAPI document itself, but the browsable UI is registered only behind the fail-closed enableDocsUi gate in src/app.ts, which is off in production and off for an unset NODE_ENV. Shipping its assets in the production image buys nothing.",
  "devDependencies": {
    "@auto-mb/worker": "workspace:*",
    "@fastify/swagger-ui": "6.1.0",
    "@types/nodemailer": "8.0.1",
    "@types/pg": "8.15.6",
    "@types/qrcode": "1.5.6"
  }
}
