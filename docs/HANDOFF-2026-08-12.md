# Engineering handoff — 12 August 2026

## Checkpoint scope

This checkpoint preserves the product-experience overhaul, LOA/tender parsing
repairs, statutory Whitebooks integration, tax-invoice evidence hardening, and
the audit follow-up work completed through migration `0045`.

The final atomic slice in this session delivered:

- normalized, append-only Measurement Book merge provenance, with tenant RLS,
  source ownership constraints, exact un-merge restoration, export coverage,
  and compatibility backfill from the former audit payload;
- one purchase-order draft per Work/vendor pair, including route, conflict,
  contract, UI, and database enforcement;
- immutable issued purchase-order and budgetary-quotation parent facts;
- automatic reopening of a closed purchase order when cancellation of a linked
  Delivery Challan releases receipt quantity, including the approved
  cancel-and-replace path and durable audit evidence;
- preservation of `purchaseOrderLineId` in corrected replacement challans;
- organisation export wire format `export-v8`, including normalized MB merge
  provenance; and
- exact record-MB consignee shape enforcement.

## Verification at checkpoint

The disposable local `auto_mb` database on PostgreSQL port `55432` was recreated
from scratch before verification. The following checks passed against the
checkpoint source:

- server, web, and database TypeScript typechecks;
- migration contract and migration-concurrency tests: 12/12;
- Measurement Book integration tests: 61/61;
- purchase-order, budgetary-quotation, and correction integration tests: 78/78;
- organisation export/integrity integration tests: 8/8;
- tenancy and production-bootstrap integration tests: 19/19; and
- `git diff --check` plus Prettier checks for the final atomic files.

Earlier in this branch, the focused Whitebooks/tax renderer tests passed 15/15,
the combined tax-invoice/e-way-bill integration tests passed 44/44, and the
statutory tenancy/bootstrap suite passed 19/19. A full repository `pnpm verify`
has not been rerun after the final `0045` slice.

## Next work

Items 1, 3 and 4 were completed by the full-repository audit of the same
date; `docs/AUDIT-2026-08-12.md` is the record, including the findings that
audit raised on its own and the fixes made for them.

1. ~~Run the full repository verification chain~~ — done. `pnpm verify`
   passes end to end. The only repository-level fallout was `graphify-out/`
   missing from `.prettierignore`, now fixed.
2. ~~The old audit disposition document~~ — done. The source audit was
   supplied on 12 August and every finding is now mapped in
   `docs/AUDIT-DISPOSITION-2026-08-10.md`. Note that it contains **44**
   findings, not 48: its numbering reaches 48, but sections 12, 24, 25 and
   26 are absent from the document, with no tracked changes to explain
   them. Seventeen findings are fixed, eleven partial, fifteen still open,
   and one dismissed by the owner. The disposition names a ten-item
   release-blocking set; the first, organisation-defined numbering that can
   wedge a series at issue time, was the only structural integrity finding
   left entirely untouched — since fixed: scope-aware template validation,
   migration `0047`, and two-Works/two-financial-years proofs, recorded
   against finding 8 in the disposition document.
3. ~~Remaining migration/evidence probes~~ — done: true pre-`0041` staged
   upgrade, migration-ledger hash/rename/removal tests, executed
   render-ledger `TRUNCATE` refusal, and reverse-charge/render/e-way-bill
   race cases. Note that ledger rename and removal were not merely untested
   — the runner could not detect them at all, and now does.
4. ~~Close-versus-cancel concurrency proof and the correction-specific
   closed-PO fixture~~ — done. No product race existed; the lock order was
   already correct, and it is now pinned.
5. ~~Finish live Whitebooks sandbox certification~~ — sandbox half done, 12
   August: the full register/cancel lifecycle ran against the live sandbox
   through the product routes; `docs/WHITEBOOKS-SANDBOX-CERTIFICATION.md` is
   the record. The provider's fresh document bundle contains no account
   secrets (the "credentials" sheet is the shared NIC test-GSTIN list), so
   the rotation gate applies to the earlier exposure only and still guards
   `WHITEBOOKS_ENVIRONMENT=production`. Never commit provider PDFs,
   screenshots, credentials, or rendered copies. (Repository hygiene was
   re-verified clean: no credentials, PDFs or screenshots are committed.)
6. ~~Restart the local server/web processes and smoke-test
   `http://127.0.0.1:4280/`~~ — done. The next session ran on a different
   machine (Windows, no Docker), so a disposable trust-auth PostgreSQL 18
   cluster was created on `127.0.0.1:55432` with the installed native
   binaries and bootstrapped from scratch (47 migrations). Server on
   `127.0.0.1:4300`, web on `http://127.0.0.1:4280/`: page renders,
   `/api/health` ok, `/api/ready` reports database and object storage ok
   (PDF renderer and malware scanner unconfigured, as expected without
   Docker), and sign-up works end to end.
7. New, from the audit: migration `0046` and the membership-scoping,
   authentication-secret, export-isolation and cancel-locking changes all
   require the fresh human review `CONTRIBUTING.md` mandates. See the review
   section of `docs/AUDIT-2026-08-12.md` for the exact surfaces.

## Operational cautions

- Migrations `0041` through `0045` are unreleased checkpoint migrations. If any
  of them changes again, recreate only the disposable local database; do not
  forge `schema_migrations` hashes.
- Tax invoice and e-way bill provider calls remain operator-triggered. Unknown
  mutation outcomes reconcile by lookup and must never resend the mutation.
- The supplied Whitebooks documentation contains live-looking authentication
  material. Treat it as compromised until the provider confirms rotation.
