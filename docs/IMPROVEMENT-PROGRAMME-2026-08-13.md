# Improvement programme — 13 August 2026

- As-of: `main` @ `43ed973` (63 migrations, all PRs merged, combined verify green
  in an isolated worktree against a fresh database).
- Inputs: a ten-reviewer expert panel (36 dimensions, evidence-measured against
  the working tree) and one independent external review (45 dimensions, judged
  largely from contracts, specs and documentation).
- Purpose: reconcile the two reviews into one score sheet, then define an
  execution programme that a set of parallel sessions can run without stepping
  on each other.

This document is the hand-off. A session picking up any pack below should need
nothing from the conversation that produced it.

---

## 1. Reconciliation

### 1.1 Why the two reviews disagree

The independent review averages 7.5; the panel averages 6.2. The gap has one
cause, visible dimension by dimension: **the independent review scored what the
contracts and documents promise; the panel measured what the code does.** Where
the two disagree by 1.5 points or more, the panel's number stands whenever it
carries a measurement, because a measurement can be re-run and a reading of the
spec cannot. Where the independent review credited something real that the
panel under-weighted (instrumentation quality, retraction culture, the org-entry
flow), the reconciled score moves up toward it.

The clearest example: error/empty/loading states. The independent review gave
8.0, citing the state vocabulary in the contract and `docs/UX.md`. The panel
gave 5, having counted: one skeleton in the entire client, retry beside 3 of
~30 load failures, and ~75% of the 672 server refusals stating a fact with no
remedy. Both observations are true. The product _specified_ an 8 and _shipped_
a 5. Reconciled: 5.

### 1.2 Factual corrections to the independent review

Recorded so they are not re-imported into future rounds:

1. **"Shared Zod contracts" — the contracts are TypeBox** (`packages/contracts`,
   `@sinclair/typebox`), not Zod.
2. **`Workspace.tsx` is treated as a live view that "will grow one-offs." It is
   dead.** Zero importers in `src`, `test`, and `e2e`; superseded by
   `OperationsWorkspace.tsx`; 24 commits have touched it since, including one on
   2026-08-13 that propagated a permissions feature into it.
3. **"Navigation matches `docs/UX.md`" — it does not.** The spec names five
   global destinations and seven Work areas; the shipped shell has ten
   destinations in four groups and ten Work tabs named after the data model.
   Either direction may be right; they currently disagree.
4. **"Live Whitebooks uncertified" is stale.** Sandbox certification was
   completed 2026-08-12 with live IRP register/cancel evidence —
   `docs/WHITEBOOKS-SANDBOX-CERTIFICATION.md`. Production credentials and a
   production run remain open, which is what the reconciled score reflects.
5. **The accessibility gate is weaker than "blocking Playwright + axe" implies.**
   It discards all moderate findings, enables zero WCAG 2.2-specific rules
   (axe's only 2.2 rule ships disabled), and runs at a single 1280×720 viewport
   with near-empty fixtures.

### 1.3 Reconciled score sheet (45 dimensions)

Scores: I = independent review, P = panel (– where the panel did not score the
dimension separately), R = reconciled. One-line basis for each reconciliation.

| #   | Dimension                        |   I |   P |       R | Basis                                                                                                                                                                                                                                 |
| --- | -------------------------------- | --: | --: | ------: | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Visual design quality            | 7.5 |   7 |   **7** | Sticky ledger headers render behind the app header on all 59 tables (`ui/table.tsx:22` vs the 4.5rem shell header) — user-visible on the core screen.                                                                                 |
| 2   | Typography                       | 8.5 |   8 |   **8** | Scale is real and enforced, but dashboard KPI figures are not mono and Devanagari 600 is unloaded under `font-synthesis: none`.                                                                                                       |
| 3   | Design-system coherence          | 8.0 |   7 |   **7** | 958 dead lines carrying the retired aesthetic; three missing primitives with measured verbatim duplication (8× detail-list, 7× nav item).                                                                                             |
| 4   | Responsive / field usability     | 6.5 |   4 |   **4** | Measured: 48/59 tables have no scroll container; inputs at 14px trigger iOS zoom; the self-described mobile view has zero responsive prefixes in 506 lines.                                                                           |
| 5   | Overall UX                       | 7.5 |   7 |   **7** | Agreement, different emphasis.                                                                                                                                                                                                        |
| 6   | Clerk ease of operation          | 6.5 |   6 |   **6** | No password recovery exists anywhere; with mandatory TOTP that is an unrecoverable lockout.                                                                                                                                           |
| 7   | Navigation / IA                  | 8.0 |   7 |   **7** | Shipped IA diverges from `docs/UX.md` (correction 3); departure protection covers the two short editors and not `ReviewLoa`.                                                                                                          |
| 8   | Error / empty / loading          | 8.0 |   5 |   **5** | Measured: 1 skeleton, 3/30 retries, ~27 dead-end load failures, ~75% of refusals without a remedy.                                                                                                                                    |
| 9   | Onboarding / first-run           | 7.0 |   3 | **3.5** | Org entry is genuinely clean (independent is right); but zero first-run guidance exists and the default deploy shows an unactionable trust-anchor warning on the flagship screen.                                                     |
| 10  | Accessibility (WCAG 2.2 AA)      | 7.0 |   6 |   **6** | Five confirmed Level A failures; the CI gate passes the worst of them while asserting on it.                                                                                                                                          |
| 11  | API design                       | 8.0 |   6 |   **6** | 323 undeclared error-code strings; no pagination on ~50 list endpoints; version string already drifted (`/api/health` 0.1.0 vs 0.11.0).                                                                                               |
| 12  | Database schema                  | 9.0 |   8 | **8.5** | Composite `(organisation_id, id)` FKs and constraint-encoded state machines are top-decile; no domains, and the FK-index audit was one-off.                                                                                           |
| 13  | Data integrity & migrations      | 9.0 |   8 | **8.5** | The runner is near-flawless; capped by the fail-open denylist on issued-document guards (already missed `reverse_charge_applicable` once).                                                                                            |
| 14  | Backup / restore / DR            | 6.5 |   5 | **5.5** | CI restore proof is real and rare; no PITR/off-host/encryption, dump and object archive at different instants (finding 33), `AUTH_SECRET` in no recovery set.                                                                         |
| 15  | Extensibility / modularity       | 7.0 |   6 |   **6** | Registrar is a mechanism with a proof; but a new numbered document type is a ten-file change and two hand lists already disagree with the route table.                                                                                |
| 16  | Simplicity / readability         | 6.5 |   7 |   **7** | Independent reacted to file sizes; panel measured median handler 60 lines and zero `any` across 92k lines. Tail is bad but bounded.                                                                                                   |
| 17  | File / module organisation       | 7.5 |   6 | **6.5** | Package boundaries enforced; module level accretive (8 route files import from `challans.ts`; shared primitives parked in the biggest consumer).                                                                                      |
| 18  | Bloat                            | 7.0 |   6 |   **6** | ~1,005 confirmed-dead lines including two orphans in the statutory path; ~870-line IRP state-machine triplication; independent missed the dead shell.                                                                                 |
| 19  | Comments & naming                | 8.5 |   8 |   **8** | Best-in-repo writing, but six comment references are provably broken and one is inverted on a live data-seeding path.                                                                                                                 |
| 20  | Application security             | 8.0 |   8 |   **8** | Agreement, from opposite directions.                                                                                                                                                                                                  |
| 21  | Business-logic hardening         | 8.5 |   7 |   **7** | Panel found what the independent didn't: install-before-delivery has no DB backstop in either form; 5 of 11 counter tables lack the decrease guard, including `tax_invoice_counters`.                                                 |
| 22  | Production readiness             | 5.5 |   4 | **4.5** | One failure domain, backup docs promise what the script doesn't do, prod image carries the dev toolchain, nothing detects schema/artefact drift.                                                                                      |
| 23  | Observability                    | 6.5 |   3 |   **4** | Both right about different halves: the instrumentation is disciplined (independent), and nothing scrapes it, nothing alerts, and readiness reports ready with no scanner (panel).                                                     |
| 24  | CI/CD                            | 8.0 |   6 | **6.5** | SHA-pinned and well-guarded deploys; but `main` is never tested as `main`, and the topology/restore jobs run weekly, not per-PR.                                                                                                      |
| 25  | Dependency / supply chain        | 7.5 |   6 | **6.5** | 26 direct runtime deps and enforced pinning; no image scanning at all, audit non-blocking, prod image ships dev deps.                                                                                                                 |
| 26  | Test quality                     | 9.0 |   8 | **8.5** | Raw-SQL trigger attacks and hand-written mutation tests; capped by untyped web stubs (309 tests against a fiction) and a corpus test importing zero production code.                                                                  |
| 27  | Coverage vs risk                 | 8.5 |   8 |   **8** | Money/tenancy/numbering disproportionately covered; concurrency almost entirely N=2; 131 triggers, 4 named in tests.                                                                                                                  |
| 28  | Test reliability / speed         | 7.0 |   6 |   **6** | Zero sleep-based waits (good); zero flake instrumentation (bad); the named flake is a Windows `rm` retry issue, diagnosed, ~1h fix.                                                                                                   |
| 29  | Fixtures / test data             | 9.0 |   8 | **8.5** | Real-document corpora with double drift-guards and tested pseudonymisation; setup is raw admin SQL bypassing API validation.                                                                                                          |
| 30  | Domain fidelity                  | 9.0 |   6 |   **7** | The paper is modelled with rare fidelity (independent right); but the tender percentage was never applied to any rate until 0063 and AMC schedules make completion unsatisfiable on the flagship work (panel right).                  |
| 31  | Statutory / regulatory           | 7.0 |   7 |   **7** | Agreement, with correction 4 applied.                                                                                                                                                                                                 |
| 32  | Completeness vs stated scope     | 8.5 |   7 | **7.5** | Scope discipline is real; the received railway bill — the only document the agency does not author — is entirely unmodelled.                                                                                                          |
| 33  | Time saved vs spreadsheets       | 6.0 |   6 |   **6** | Agreement: strong on document production, absent on money tracking, no measured clerk study.                                                                                                                                          |
| 34  | Backend performance              | 5.5 |   5 |   **5** | Measured: MB loader 33,669 probes/446ms; RLS helper 4.1× in filter position; 19 per-row write loops; 59 unpaginated lists.                                                                                                            |
| 35  | Frontend dense-table performance | 5.0 |   4 |   **4** | Measured: zero `React.memo`, zero virtualisation, one 872 KiB chunk, keystroke re-renders 129 controlled inputs.                                                                                                                      |
| 36  | Scalability headroom             | 5.5 |   4 |   **4** | Measured: pool of 5, dashboard 881ms at 412k items → ~5 req/s ceiling; all growth curves unbounded.                                                                                                                                   |
| 37  | Tenant isolation / RLS           | 8.5 |   – | **8.5** | Panel's security review corroborates: FORCEd RLS, catalog-asserted, allowlist complete.                                                                                                                                               |
| 38  | Authorization & privileges       | 8.5 |   – |   **8** | Solid, minus: a new authority column is silently MFA-exempt (no exhaustiveness link), two membership writes lack the org predicate.                                                                                                   |
| 39  | Issued documents & money         | 8.0 |   – | **7.5** | Immutability is real but denylist-shaped; taxable value is enforced in exactly one layer (route), unlike every security control (two).                                                                                                |
| 40  | Document generation              | 7.5 |   – | **7.5** | Accepted.                                                                                                                                                                                                                             |
| 41  | External integrations            | 6.5 |   – |   **7** | Corrected upward: sandbox certification exists with live evidence; production run open.                                                                                                                                               |
| 42  | Developer experience             | 8.0 |   – |   **8** | Accepted.                                                                                                                                                                                                                             |
| 43  | Documentation honesty            | 8.5 |   – | **6.5** | The retraction culture is real, but ops docs promise encryption/PITR/retention the script lacks, OPERATIONS describes a removed rate-limiter design, ADR-0008's load-bearing premise is false, and six comment references are broken. |
| 44  | Parser / AI safety               | 9.0 |   – | **8.5** | The lock/confirm/discard discipline is exemplary; capped because extraction fixtures are never re-derived and Poppler is unpinned and different between CI (apt) and prod (apk).                                                      |
| 45  | Maintainability / bus factor     | 6.5 |   – | **6.5** | Accepted. Bus factor is 1 and that is an owner fact, not a code fact.                                                                                                                                                                 |

**Reconciled mean: ~6.6** (independent 7.5, panel 6.2). The shape matters more
than the mean: the core (schema, integrity, tests, tenancy, parser safety) sits
at 8–8.5 while the wrapper (operations, onboarding, performance, mobile) sits
at 3.5–5.

### 1.4 The four recurring findings

Every pack below traces to one of these.

1. **Invariants established once, never converted into standing checks.** The
   immutability column list, FK-index coverage, counter monotonicity, upload
   throttle scope, MFA authority coverage, dead-file detection. The house style
   for the fix already exists (the catalog-driven RLS / privilege / export
   drift tests) and is applied unevenly.
2. **Security is enforced twice; money once.** Tenancy has RLS + explicit
   predicates + an enumerating test. The taxable value has a route function.
   The install-vs-delivered floor has a route check and no trigger.
3. **Docs claim more than the code does.** Backup encryption/PITR, the
   rate-limiter paragraph, ADR-0008's compose-churn premise, six broken comment
   references (one inverted on the unit-seeding path).
4. **Specified but not shipped.** Nine UX states specified, ~two implemented;
   five global destinations specified, ten shipped; a11y gates defined in
   `package.json` and wired into nothing.

---

## 2. The programme

### 2.1 Ground rules for every pack

- **One pack = one branch = one PR.** Branch names `pack/p<NN>-<slug>`.
- **File ownership is exclusive.** The owner column below is a contract; a pack
  must not touch another pack's files. Shared hotspots (`package.json`,
  `scripts/check-architecture.mjs`, `ci.yml`) are assigned to exactly one pack
  per wave; later packs rebase.
- **Every pack lands its regression guard in the same PR as its fix.** A pack
  that fixes without guarding is incomplete; the guard is the deliverable.
- **Verify in isolation.** Full `pnpm verify` before the PR, in a detached
  worktree pinned to the merge-base, against a freshly created database — never
  the shared checkout, never the shared `auto_mb` DB. (Both contaminations
  produced false verify results on 2026-08-13; this is the lesson, encoded.)
- **Review sensitivity is marked per pack.** Packs touching migrations, auth,
  uploads, money, permissions or infrastructure are opened as PRs and **held
  for owner review** per `CONTRIBUTING.md` — they are never self-merged.
  Docs/test/UI-only packs merge when green.
- **Migration numbers are reserved here** to prevent the collisions that
  occurred twice in the previous round: **0064** → P2, **0065** → P2,
  **0066** → P14, **0067** → P15, **0068** → P16, **0069** → P17,
  **0070** → P6. A pack that does not ship its migration returns the number
  by editing this file. P6 was not expected to need one; it takes 0070
  rather than 0066 so the wave-3 reservations are left undisturbed, and the
  gap is harmless because the runner applies any unapplied id regardless of
  relative order. **0071** → P19, taken at the time that pack finalised
  rather than reserved here: its row carried no migration, and it turned out
  to need one. **0072** → P18, on the same footing and for the same reason:
  its row carried no migration either, and the queue table needed one. P18
  chose 0072 while 0071 (P19) and 0067 (P15) were still open pull requests,
  so the rule it followed was "the next id no other pack holds", not "the
  next id on disk" — which would have collided with both.

### 2.2 Wave 1 — guards and closures (all seven packs runnable in parallel)

| Pack                               | Scope                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Owns (exclusively)                                                                                                                                                                                                           | Effort    | Unlocks                                                                     | Review                        |
| ---------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | --------------------------------------------------------------------------- | ----------------------------- |
| **P1 Dead code + knip**            | Delete `views/Workspace.tsx`, `views/Dashboard.tsx`, `gsp/ewb-payload.ts`, `gsp/eway-by-irn.ts`, dead hash helpers; fix the six broken comment references (incl. the inverted unit-list comment in `needs-review.ts:402`); correct ADR-0008's compose-churn claim; add `knip` with frozen baseline + comment-reference linter (`scripts/check-comment-refs.mjs`) into `pnpm verify`.                                                                                                                                                                     | the four deleted files, `knip.json`, `scripts/check-comment-refs.mjs`, `adr/0008`, root `package.json` (wave-1 owner)                                                                                                        | ~1 day    | Bloat 6→7, Comments 8→8.5, Docs-honesty +0.5                                | merge when green              |
| **P2 DB invariant guards**         | New `packages/db/test/issued-immutability-coverage` (guard ROW list ∪ declared-mutable ≡ catalog, per issued table); trigger census + counter-table rule in `migration-contract.test.ts`; FK-index catalog test; **migration 0064**: `guard_counter_decrease` on the four unprotected counter tables + unique `(organisation_id, fy_label, sequence_number)` on tax invoices; **migration 0065**: `CREATE DOMAIN sha256_hex / money_amount / quantity_amount` + `work_items_live` view; org predicate on the two membership writes in `identity.ts`.     | `packages/db/test/*` (new files), `packages/db/migrations/0064-0065`, `apps/server/src/routes/identity.ts`                                                                                                                   | ~2 days   | Data integrity 8.5→9, Biz-logic 7→7.5, Schema 8.5→9                         | **HOLD — migrations + money** |
| **P3 Table & mobile primitive**    | `ui/table.tsx`: `scroll` default true, `tabIndex=0` + `role="region"` + label on the wrapper, `--header-h` token + sticky offset (incl. `schedule-section` stacking); `globals.css`: 16px inputs on coarse pointers, 24px checkboxes, `--input` ≥3:1; Playwright `projects` (1280 / 768 / **320**) + `scrollWidth <= innerWidth` assertion + sticky-occlusion assertion; widen the axe gate (drop severity filter, enable wcag22 tags + `target-size`).                                                                                                  | `apps/web/src/ui/table.tsx`, `ui/schedule-section.tsx`, `apps/web/src/globals.css`, `apps/web/playwright.config.ts`, `apps/web/e2e/*`                                                                                        | ~1.5 days | Responsive 4→6, Visual 7→7.5, A11y 6→6.5, and un-blinds two gate dimensions | merge when green              |
| **P4 Server upload/authz hygiene** | Derive upload throttle from registrar `bodyLimit` (closes the live unthrottled variation-order path); shared `consumeUpload()` replacing 7 copied magic-byte blocks + upload-inventory test; production boot assertion for `CLAMAV_HOST`; MFA exhaustiveness (`Record<DocumentAuthority, true>` + `can_%` column census test).                                                                                                                                                                                                                           | `apps/server/src/app.ts` (wave-1 owner), `upload-guards.ts`, `mfa-policy.ts`, `apps/server/test/upload-inventory*`, `test/mfa-policy*`                                                                                       | ~1.5 days | Security 8→8.5, Extensibility 6→6.5                                         | **HOLD — auth/uploads**       |
| **P5 Recovery + unsaved work**     | `sendResetPassword` + SignIn flow (the only unrecoverable failure in either review); `ReviewLoa` dirty tracking routed through `requestDeparture`; `check-architecture` rule: `setView(` outside `navigate`/`requestDeparture` fails.                                                                                                                                                                                                                                                                                                                    | `apps/server/src/auth.ts`, `apps/web/src/views/SignIn.tsx`, `views/ReviewLoa.tsx`, `views/OperationsWorkspace.tsx` (wave-1 owner)                                                                                            | ~1 day    | Clerk ease 6→7, Navigation 7→7.5                                            | **HOLD — auth**               |
| **P6 Ops gates**                   | `/api/ready` 503s when the applied-migration ledger is behind the image's migration dir; deploy readiness gate gains the component greps CI already has; healthchecks for server/caddy/gotenberg/clamav + log rotation + resource limits; Trivy on built images + digest-pin bases; prod image `--prod`, compiled not `tsx`; backup: encrypt, include `AUTH_SECRET`, fix finding-33 snapshot skew, off-host copy; commit `alert_rules.yml` + prometheus/alertmanager services; rewrite the backup/RUNBOOK paragraphs to state only what the script does. | `apps/server/src/routes/health.ts`, `deploy/**`, `scripts/backup.sh`, `scripts/restore.sh`, `.github/workflows/deploy.yml`, `docs/OPERATIONS.md`, `docs/RUNBOOK.md`                                                          | ~3 days   | Prod-readiness 4.5→6, Observability 4→6, Backup 5.5→7, CI/CD +0.5           | **HOLD — infrastructure**     |
| **P7 Test infrastructure**         | Test census (`scripts/check-test-census.mjs`, ratchet: counts may only rise, skips need reasons); CI flake detector (`--retry=1` + JSON reporter + fail on pass-after-retry); fix `backup-restore` `rm` (`maxRetries`) + tighten its bare `.rejects.toThrow()`; type `stubApi` payloads against `ApiClient`; pin Poppler to one exact version in CI **and** `Dockerfile.server` + one PDF→text round-trip test; nightly Stryker scoped to 4 invariant files.                                                                                             | `scripts/check-test-census.mjs`, `.github/workflows/ci.yml` (wave-1 owner), `.github/workflows/mutation-nightly.yml`, `packages/db/test/backup-restore*`, `apps/web/test/views/helpers.tsx`, `apps/server/test/loa-extract*` | ~2 days   | Test reliability 6→7.5, Test quality 8.5→9, Parser safety 8.5→9             | merge when green              |

### 2.3 Wave 2 — states, performance, contracts (after wave 1 merges)

| Pack                               | Scope                                                                                                                                                                                                                                                                                                                                                                                                              | Owns                                                                                                                                                                                                  | Effort  | Unlocks                             | Review                                           |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ----------------------------------- | ------------------------------------------------ |
| **P8 State primitives + remedies** | `ui/state.tsx` (`Skeleton`, `EmptyState`, `ErrorState` with **required** `onRetry`); convert the ~27 dead-end views; render `details.existingRecordId` as a link in `UploadLoa`; `remedy` field on the API error envelope + backfill the top 40 refusals; generated per-view state-coverage test.                                                                                                                  | `apps/web/src/ui/state.tsx`, all `views/*` state branches, `packages/contracts/src/errors.ts` (shared with P12 — P12 rebases), `apps/web/test/views/state-coverage*`                                  | ~4 days | Error states 5→7.5, UX 7→7.5        | merge when green                                 |
| **P9 Onboarding**                  | `SetupChecklist` on zero-Works dashboard (reuse the `WorkBillingReadiness` pattern); five Masters empty states with purpose + auto-opened create; CCA India root bundle in `deploy/` + default `AUTO_MB_PDF_TRUST_ANCHORS` + `check-config` rule; role-aware Works CTA; `DateField` wrapper with DD/MM/YYYY hint; work-code `Hint` + `maxLength`; first-run no-dead-end test.                                      | `views/OperationsDashboard.tsx`, `views/Masters.tsx`, `views/Works.tsx`, `ui/date-field.tsx`, `deploy/trust-anchors/**`, `scripts/check-config.mjs`                                                   | ~3 days | Onboarding 3.5→6.5, Clerk ease +0.5 | trust-anchor part **HOLD — deploy**; rest merges |
| **P10 Frontend performance**       | `React.memo` row components + `Map` lookups (`ChallanDetail`, `MeasurementBooks`); fix `ChallanEditor` keystroke clone; `React.lazy` at the view switch; bundle budget (gzip ≤220 KB ratchet) in verify; `table-perf.spec.ts` (129-item fixture, no long task >200ms, ≤3 commits/keystroke); cache `Intl.DateTimeFormat`.                                                                                          | `views/ChallanEditor.tsx`, `views/ChallanDetail.tsx`, `views/MeasurementBooks.tsx`, `lib/format.ts`, `apps/web/scripts/check-bundle-size.mjs`, `e2e/table-perf.spec.ts`                               | ~3 days | Frontend perf 4→6                   | merge when green                                 |
| **P11 Backend performance**        | MB `loadItemInputs` six-lateral → grouped aggregate; bulk-insert the 19 write loops; dashboard pre-aggregation; pool `max` 5→20 + `requestTimeout`/`keepAliveTimeout`; query-count budgets, EXPLAIN plan assertions, buffer-count ratchets (`scale-budget` test with committed thresholds); stream `/api/export` with a 200 schema.                                                                                | `apps/server/src/routes/measurement-books/**`, `routes/dashboard.ts`, `routes/export.ts`, `routes/loa.ts` (confirm loop), `apps/server/src/main.ts`, `apps/server/test/query-*`, `test/scale-budget*` | ~4 days | Backend perf 5→7, Scalability 4→5   | **HOLD — money paths (MB compute)**              |
| **P12 API contract**               | `ERROR_CODES` const union in contracts, `httpError` retyped (typecheck-enforced vocabulary; dedupe the 323); keyset pagination on the six largest lists (adopt the timeline shape); fix `/api/health` version to read `API_VERSION`; route-inventory additions (429 in every response schema, unpaginated-list allowlist).                                                                                         | `packages/contracts/src/errors.ts` (after P8), `packages/contracts/src/pagination*`, `apps/server/src/http.ts`, the six list routes, `apps/web/src/api.ts` (wave-2 owner)                             | ~4 days | API 6→8                             | merge when green                                 |
| **P13 Accessibility completion**   | Masters tablist: real keyboard pattern or demote to the honest nav; per-view `document.title`; shared `ui/dialog.tsx`/`ui/confirm.tsx` extracted from the two correct implementations, migrate the ~12 broken confirm sites; async-arrival announcements in `ReviewLoa`; skip link; source-invariant a11y greps (caption-per-DataTable, tablist⇒onKeyDown, aria-expanded⇒aria-controls); `eslint-plugin-jsx-a11y`. | `views/Masters.tsx`, `ui/dialog.tsx`, `ui/confirm.tsx`, `App.tsx`, `apps/web/test/a11y-invariants*`, `eslint.config.js`                                                                               | ~3 days | A11y 6.5→8                          | merge when green                                 |

### 2.4 Wave 3 — product builds (each needs an owner decision or review first)

| Pack                               | Scope                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Migration | Effort             | Why it matters                                                                                                                          |
| ---------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ------------------ | --------------------------------------------------------------------------------------------------------------------------------------- |
| **P14 Received railway bill**      | New inbound document kind on the `loa_documents` machinery (storage, magic bytes, scan, RLS, 0060 signature-verdict columns all exist); link to finalized MB by measurement **sequence** with L2→FL2 normalisation and line-wrap rejoin (traps recorded in `apps/server/test/fixtures/railway-settlement/corpus.json`); carry bill number/date/amount + three-signature verdict; verdict gates MB closure and invoice settlement per the owner's 2026-08-13 gating rulings (intact + chains to CCA India, expiry ignored). **Landed in a different shape than this row proposed** (§2.8, PR #68): the bill lives in its own table `received_railway_bills`, not as a `loa_documents` kind — 0040's source-shape CHECK requires every non-`'loa'` kind to hang off a parent LOA row _and_ carry a matched tender identity, and a received bill has neither; the machinery is reused, the table is not (0058's precedent). The L2→FL2 normalisation is a **test** helper, because production links by letter number + sequence and the ledger token is handled by construction. And the verdict requires three **distinct** signing certificates, compared by issuer + serial — a strengthening ratified by the owner on 2026-08-14. | **0066**  | ~1 week            | Both reviews' #1 adoption blocker: the only chain document the agency does not author. Closes the loop the settlement corpus documents. |
| **P15 Payment & deduction record** | Amount received against a bill with deduction breakup (GST TDS §51, IT TDS §194C, retention/SD, penalties); outstanding-with-railway view.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **0067**  | ~3 days            | "Paid" is currently a checkbox with no amount; this is the spreadsheet operators would still keep.                                      |
| **P16 AMC schedule category**      | Fifth payment category (or per-item completion basis) so `Year`-unit AMC schedules neither block completion nor require fabricated challan lines; completion predicate honest on PL-270. **The category won, and the alternative is now closed** (§2.8, PR #67): `docs/PRODUCT.md` §5.4 records that there is deliberately no second classification axis, because two axes can contradict each other and the contradiction has no meaning. `AMC` is discharged by **certification** through `pac_certificates`, capped at the **sanctioned** quantity rather than the installed total, in the route and again in a database trigger.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **0068**  | ~3 days            | Completion is currently unsatisfiable on the flagship corpus work (~15% of its value is AMC).                                           |
| **P17 RLS memoisation**            | ADR first: bind-time membership verification with policies reading the verified GUC (4.1× measured win in filter position) **without weakening the tenancy floor**; implement only after the ADR is approved. **Superseded in design by ADR-0010** (accepted 2026-08-14): the verified GUC is rejected, because user-defined GUCs carry no ACL — `SET app.verified_organisation_id` is a SQL command, not a revocable function call, so any path to arbitrary SQL as the application role would forge the binding. The accepted shape keeps the membership probe inside the existing definer helper and moves every policy call into scalar-subquery (InitPlan) position, with an additive `app_private.bind_tenant` that fails a wrong binding closed with Auto-MB's own SQLSTATE `28A01`.                                                                                                                                                                                                                                                                                                                                                                                                                                        | **0069**  | ADR ½ day + ~1 day | The single largest uniform backend win; touches the security kernel, so design-before-code.                                             |
| **P18 Worker wiring**              | First real job in `apps/worker` (`FOR UPDATE SKIP LOCKED` queue table): move Gotenberg render, ClamAV scan, Poppler extraction, signature verification off the request path; honours the ADR-0008 tripwire. **Two of the four moved** (§2.9, PR #75, ADR-0011): Poppler extraction and signature verification, sharing one job on the LOA intake path. The ClamAV scan **stays synchronous** — it is an admission gate, and moving it would trade "nothing unscanned is ever stored" for the weaker "nothing unscanned is ever served" across every download, render, export and PDF path. The Gotenberg render is **deferred with its design recorded** — seven persisting render routes, each building its HTML in its own module; the shape that fits ADR-0011 is the request path writing the built HTML to storage and enqueueing a reference. The row also carried no migration; the queue table needed one, and it took **0072** (§2.1).                                                                                                                                                                                                                                                                                    | **0072**  | 1–2 weeks          | Scalability 5→7; makes a second API instance meaningful.                                                                                |
| **P19 Confirmed-Work supersede**   | Approval-gated discard-and-reconfirm for a confirmed Work with no downstream documents (soft-delete + successor with provenance), so the remedy 0063's own header prescribes is executable.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **0071**  | ~4 days            | The correction deadlock: today a confirmed Work with wrong extracted data has no exit.                                                  |

Deliberately **not** scheduled (owner-money or externally gated): replica /
managed Postgres, registry + protected environments, staging + DAST, outbound
DSC signing (ADR-0009's two procurement blockers), IRP production
certification, Aadhaar eSign evaluation.

> 2026-08-14 update: the outbound-signing and eSign rows above resolved
> together into **ADR-0012** (hybrid: eSign default, kiosk-held DSC by
> exception), decided after the owner confirmed the signer approves from
> a phone and the token stays at one private machine. The vertical is a
> wave-4 pack, gated on ASP onboarding with an ESP and the TSA contract.

### 2.5 Parallel execution plan

**Sessions.** Each pack is one session in its own worktree
(`git worktree add ../amb-p<NN> -b pack/p<NN>-<slug> origin/main`). Wave 1 =
up to seven concurrent sessions; the file-ownership table is the collision
contract. A session brief needs only: this file, the pack row, and the two
standing rules (verify isolated; high-risk PRs are opened, not merged).

**Shared-file owners per wave** (everyone else rebases): wave 1 —
`package.json` → P1, `check-architecture.mjs` → P5, `ci.yml` → P7,
`app.ts` → P4. Wave 2 — `contracts/errors.ts` → P8 then P12, `api.ts` → P12.

Wave 3 — the shared surfaces here are business rules rather than files,
because three packs land in the same completion-and-settlement area:

- **The R8 completion predicate** (`apps/server/src/routes/work-completion.ts`,
  `WorkCompletionRequirement` and `UnfinishedWorkItem` in
  `packages/contracts/src/works.ts`) → **P16**. P14 does not touch it and
  rebases onto P16's merged result.
- **The MB-closure and invoice-settlement gates** → **P14**. P16 does not
  touch those, and P15's payment record builds on P14's settlement shape.
- **The item payment-category vocabulary** (`WORK_ITEM_PAYMENT_CATEGORIES`,
  the `work_items` and `payment_matrices` CHECKs) → **P16**, which adds
  the fifth value. A later pack adding a sixth rebases.
- **`docs/PRODUCT-SPEC.md` does not exist in this repository.** The
  architecture-independent product spec is `docs/PRODUCT.md`. Every pack
  edits its own section of it in the same commit as the behaviour, and
  conflicts are resolved by merge order — the later pack re-reads the
  merged section rather than replaying its own.
- **`packages/contracts/src/errors.ts`** stays additive: a pack appends
  its own codes to `ERROR_CODES` in sorted position and touches no other
  line, so concurrent packs conflict only when they add adjacent codes.

**Merge order within a wave:** smallest diff first (P1 → P3 → P5 → P7 → P4 →
P2 → P6 for wave 1), each followed by branch-update of the remainder. After
each wave: combined `pnpm verify` from a pinned worktree + fresh database,
then re-run the graphify build so the knowledge graph tracks the tree.

**Definition of done per pack:** fix merged **and** guard merged **and** the
guard demonstrably fails on the pre-fix tree (state the command in the PR) —
plus the score dimension it claims, re-checked in the next review round.

**Projected effect** (taking the reviewers' own unlock estimates): wave 1
≈ +0.4 on the reconciled mean with every guard in place; waves 1–2 ≈ 6.6 →
**~7.4** with no dimension below 5; wave 3 moves adoption rather than scores.

### 2.6 Wave 1 landing record (2026-08-13)

All seven packs merged the same day they were briefed, plus one hotfix and
three side-fixes the wave surfaced. Verification for every pack: isolated
worktree, fresh database, guard-fails-on-pre-fix proof stated in the PR.

| Pack | PR  | Notes beyond the brief                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ---- | --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P1   | #49 | Dead hash helpers were `deliveryChallanHash`/`searchHash`; the inverted comment claimed a migration seeds units when `masters.ts` seeds from `CANONICAL_UNIT_NAMES`; applied-migration comments are checksum-sealed, handled via a `FROZEN_STALE` linter entry. The new linter caught P5's edits to the dead shell during rebase.                                                                                                                                         |
| P2   | #50 | Resolved a contradiction in this document: `extension_request_counters` must NOT carry the decrease guard — its 0029 top-of-sequence delete path decrements by design; its invariant is gaplessness. §1.3 row 21's "5 of 11" over-counted (correction 4 in §3). Migrations 0064/0065 shipped as reserved.                                                                                                                                                                 |
| P3   | #47 | The pack row's "scroll default + sticky offset" is impossible on one scrollport (`overflow-x: auto` forces a scroll container); resolved with a `--sticky-inset`-capped scrollport, measured in-browser. Found the e2e suite red on main since hash routing — invisible because Actions was disabled.                                                                                                                                                                     |
| P4   | #46 | The derived throttle closed a live unthrottled variation-order upload path. The old path-list throttle also fired on unmatched paths; the derived one covers registered routes only (accepted).                                                                                                                                                                                                                                                                           |
| P5   | #43 | Password recovery via SMTP (nodemailer); production boot now requires `SMTP_URL`/`MAIL_FROM`. PR #45 added them to the prod compose.                                                                                                                                                                                                                                                                                                                                      |
| P6   | #53 | Backup encryption is envelope (RSA-OAEP + AES-256-CBC), integrity is checksum-based not authenticated (stated posture). Local compiled-artifact run caught nodemailer inlined into the ESM bundle pre-production. CI caught two more defects before merge: a Windows-shaped openssl probe, and the ledger-read grant living only in bootstrap (fixed as migration 0070 — reserved out of order, see §2.1). Off-host destination and alert channel remain owner decisions. |
| P7   | #48 | Amended before merge to conserve Actions minutes (owner decision): census per-PR in its own workflow, mutation weekly, flake detection manual until production deployment. Branch protection must require `census`, not the path-filtered `verify`.                                                                                                                                                                                                                       |

Side-fixes: #44 (tax-invoice `ackDate` fixture time bomb — failed on an
untouched tree from 2026-08-13T09:00Z), #51 (same hazard class in
credit-notes text forms), #52 (replica-role suite cleanups left FK orphans
breaking the restore proof; replaced by a shared tenant-closure delete +
FK census).

Process notes for wave 2, learned the hard way: the merge train ran
review-first except once (recorded on PR #46); GitHub Actions was enabled
mid-wave and immediately caught two real defects local verifies missed;
seven concurrent sessions saturate this machine — stagger heavy packs, and
treat timeout-shaped failures as contention proven per-package.

### 2.7 Wave 2 landing record (2026-08-13)

Same verification standard as wave 1: isolated worktree, freshly created
database, full `pnpm verify`, and a guard proved to fail on the pre-fix
tree with the command stated in the PR.

| Pack | PR  | Notes beyond the brief                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ---- | --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P9   | #55 | The CCA publishes four current roots; only three are bundled. `CCA India 2015 SPL` as served fails its own self-signature check (`openssl verify -check_ss_sig` → `certificate signature failure`), reproduced on three downloads from both hostnames, so it is documented and left out rather than shipped unverified. Provenance is one channel (TLS from `cca.gov.in`) corroborated for `CCA India 2022` by the fingerprint already in `docs/OPERATIONS.md` §8; the out-of-band `verifyroot@cca.gov.in` confirmation stays an operator step and the docs now say so. The Masters "auto-opened create" half of the row already existed (`MasterForm startOpen`); what was missing was the purpose sentence, so the empty states name it and point at the open form.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| P10  | #56 | "≤3 React commits per keystroke" does not separate the two trees — React batches a keystroke into one commit before and after the fix — so the spec measures ROWS RE-RENDERED per keystroke as well, counted by fiber identity through React's own devtools hook: 130 before, 2 after (the item row and the column heading). Long tasks never exceeded the browser's 50ms reporting floor on either tree, so that assertion is a standing guard rather than a proof. The bundle guard carries two lines: the programme's 220 kB budget, which the pre-fix tree fails at 220.74 kB, and a 115 kB ratchet, because 220 kB has a hundred kilobytes of slack under the code-split payload. Touched two files outside the pack's list — see the PR for both.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| P8   | #57 | The "~27 dead-end load failures" resolved to a measurable set: 27 views carry a mount load path, and the new guard fails on 20 of them plus SerialLookup's missing wait state on the pre-fix tree. The other 6 are P9's (`OperationsDashboard`, `Masters`, `Works`) and P10's (`ChallanDetail`, `ChallanEditor`, `MeasurementBooks`); they are recorded as exemptions in `state-coverage-cases.tsx` with the owning pack named, and deleting the exemption is how that pack claims the view. The `remedy` backfill landed as a code-keyed catalog (`apps/server/src/remedies.ts`) rather than as edits to route call sites — the same advice written forty times drifts, and the routes were being edited concurrently by P11. Two failure states keep an inline refusal with no retry on purpose: a 403 is the permission-limited state `docs/UX.md` separates from a service failure. `Settings` gained a state it never had — an unreadable number-series table used to render as an empty one, which is the empty-register lie about a table.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| P11  | #58 | The review's "19 write loops" measured 20 in-loop writes on the request path, converted here, plus 13 left exempt with reasons frozen in `test/query-write-loop-census.test.ts` (the v1 import CLI writes each row in its own savepoint by design; provider-operation lease recovery branches to a different table per row). Two findings worth carrying forward: postgres.js types a parameter array from its FIRST element, so an array of JS booleans is declared `bool` and refused, and a text array cast straight to `boolean[]` arrives all-false — `::text[]::boolean[]` is the shape that works. And the dashboard's grouped form reads slightly MORE buffers than the laterals at fixture scale (470 against 303); its win is the per-Work re-execution, which the plan-shape assertion measures directly, so the buffer number is a ratchet rather than a claim.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| P13  | #59 | The Masters tablist was DEMOTED rather than given a keyboard pattern: every category is its own address (`#/masters/units`), opening one pushes a history entry, and the rail opens one directly — that is navigation, and `views/WorkDetail.tsx` had already settled the same question the same way with a `nav` + `aria-current="page"`. So the tree now holds no tablist at all, and the `tablist ⇒ onKeyDown` grep guards the next one instead of the one that was there. Two of the three source greps failed on the pre-fix tree; the caption grep did not, and is stated in the file as holding a property rather than repairing one (proved by deleting a caption). `eslint-plugin-jsx-a11y` lands `strict`, not `recommended`, with three file-scoped reasoned exemptions and no warning baseline: `no-autofocus` for the three sole-field sign-in steps, `no-noninteractive-element-interactions` for `ui/dialog.tsx` (a modal owning Escape and Tab is the point), and `no-noninteractive-tabindex` for `ui/table.tsx`, where the lint rule and axe's `scrollable-region-focusable` contradict each other and axe wins. The recorded 320px cause for Settings was wrong: `OrganisationAccessSettings` contributed 300px, but the driver was a `<select>` whose widest OPTION measured 381px in Chromium, and the general fix is `w-full` on `ui/card.tsx` — several views had already discovered it one at a time. Touched five files outside the pack list (`ui/card.tsx`, `globals.css`, `AppearanceSettings.tsx`, `OrganisationAccessSettings.tsx`, `e2e/responsive.spec.ts`) and root `package.json` for the plugin dependency, which cannot be resolved from a workspace package.                                                                                                                                                                                                                                                                                                                                                                                                          |
| P12  | #60 | The dedupe's own count moved twice. 323 was what a `httpError(` census could see; 14 were second spellings and collapsed, leaving 309 — and the type checker then found **56 more the census never could**, raised through the statutory adapter, the snapshot and IRN evidence errors, and the shared master-route helper, five of them assembled by string interpolation (`WHITEBOOKS_${error.code}`) so no search for their names could ever have found them. Those were already reaching clients as 409s and 502s; 369 codes are declared now. Three findings beyond the row. (1) "Fix the `/api/health` version" was already recorded as **fixed** in `AUDIT-DISPOSITION-2026-08-10` row 44 — the OpenAPI copy was fixed, the probe's own copy was not, and that row is corrected here rather than left claiming more than it delivered. (2) The obvious keyset shape — read the sort key out as text, send it back as `$n::timestamptz` — loses microseconds: `21:50:41.527771+05:30` reached the server as `21:50:41.527+05:30`, because the driver re-encodes a parameter it types as `timestamptz` through a JavaScript Date. On the ascending site-measurement register that repeated one row forever, which is how the walk test caught it; on a descending one it would silently skip. Every cursor in the tree now reads its position inside the comparing statement instead, including the timeline's — which was TESTED and does not lose them in the shape it shipped in, so that half is hardening, and the landing note says so rather than claiming a fix it did not make. (3) The delivery-challan register's scope predicate was `A or B and C`; adding the page predicate after it bound tighter than the `or`, so full-scope members got no page at all — caught by the same walk test, which is the argument for walking a register one row at a time rather than asserting that a page comes back. 429 is declared once in `createTenantRouteRegistrar` rather than in ~200 hand-kept response maps, because the throttle is a global hook and only its rule selection is narrow. |

Process notes for wave 3:

- `docs/OPERATIONS.md` §8 asserted "why they are not shipped in the image".
  Shipping a default made a documentation sentence false, and updating it
  was part of the change, not follow-up (recurring finding 3). A pack that
  ships an artefact should expect to own the paragraph that explains its
  absence.
- Wave 1's lesson repeated exactly: CI failed where a local verify passed.
  `apps/web/test/views/masters-settings.test.tsx` awaited
  `findByRole('heading', { name: 'Settings' })` before reading the form,
  but that heading renders during the profile fetch as well, so all four
  of its Settings cases were racing the mock and had been passing by
  timing. P9's extra test file changed the scheduling enough to lose the
  race on the runner. The class of bug — awaiting an element that exists
  in the LOADING state — is worth a grep before wave 3 writes more view
  tests. _Resolved: after the class bit a second time (`search.test.tsx`
  on P14, PR #68), the grep became a standing guard —
  `apps/web/test/views/loading-anchor-census.test.ts` derives the
  loading-visible heading names from every view's loading branch and
  fails any view test that anchors an await on one._

### 2.8 Wave 3 landing record — tranche A (2026-08-14)

Three product packs and one side-fix, merged in this order: **#66** (P17,
migration 0069) → **#69** (the loading-anchor census) → **#67** (P16,
migration 0068) → **#68** (P14, migration 0066). Every one of them
required fresh human review under `CONTRIBUTING.md` — P17 touches the
tenancy kernel, P16 money and a migration, P14 uploads, money, a
migration and the gating of an issued document — and each was opened
rather than merged, then merged after that review.

The verification standard is wave 1's and wave 2's with one honest
amendment: **per-package runs against a freshly created database, plus
CI**. The combined `pnpm verify` is no longer claimed (see the process
notes below).

| Pack | PR  | Notes beyond the brief                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ---- | --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P17  | #66 | The ADR came first and was accepted by the owner on 2026-08-14; the pack implements it. Migration 0069 rewrites **64** tenant policies into scalar-subquery (InitPlan) position — `organisation_id = (SELECT app_private.current_organisation_id())` — carrying 123 calls to that helper and 2 to `current_user_id()`, and adds `app_private.bind_tenant`, a definer function that refuses a binding the membership probe does not support with Auto-MB's own SQLSTATE `28A01`, surfaced as `TenantBindRefusedError` (`packages/db/src/tenant.ts`) and mapped to the existing not-a-member refusal in `apps/server/src/tenant-context.ts`. The review pass raised **ten** findings and all ten are addressed, but only **eight by code**. One is deliberately **kept**: `assertBoundThen`'s read-back is now unreachable through that module, because `bind_tenant` refused the same condition a statement earlier, and it stays as documented defence-in-depth — whether a GUC written by `set_config` inside a function carrying its own `SET search_path` survives the function boundary is PostgreSQL implementation behaviour rather than a standard promise, verified on 18.4 and on the 17 CI image, and the call-site comment says exactly that rather than making the choice silently. One is **documentation only**: `docs/RUNBOOK.md` §3 gained "Migrations that take heavy locks: 0069" — each `ALTER POLICY` takes `ACCESS EXCLUSIVE`, the runner applies a migration in one transaction, so all 64 locks accumulate until commit; run it in a quiet window, `lock_timeout = '2s'` aborts rather than queues, retry is safe, and stopping the app containers for a few seconds beats raising the timeout. P11's aggregate ratchet was recalibrated **4× → 2×** in `apps/server/test/query-aggregates.integration.test.ts` — not `scale-budget.integration.test.ts`, whose thresholds are absolute ceilings — and **both** measurements are written into the file rather than only the one that survived: bare policies `current 9,418–9,939` against `retired 46,066–55,396` (4.9–5.6×), InitPlan policies `current 3,448–4,387` against `retired 13,426–15,782` (3.1–4.6×). One piece of drift, found by this close-out and **fixed in the same pull request as this record**: the ADR body still said the bind raises `28000` in three places, because the identifier moved to `28A01` during the review round and the decision record was not carried forward with it. `28000` is PostgreSQL's own `invalid_authorization_specification`, so the choice is load-bearing rather than cosmetic — a caller mapping it to "not a member" would answer a cluster-wide authentication outage with tenant-shaped 403s — and the ADR now states the decision as taken rather than as first drafted.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| P16  | #67 | The row offered "fifth payment category **or** per-item completion basis"; the category won, and `docs/PRODUCT.md` §5.4 records why the alternative was rejected rather than merely not chosen — two classification axes can contradict each other and the contradiction has no meaning. `AMC` joins `WORK_ITEM_PAYMENT_CATEGORIES` (`packages/contracts/src/payment.ts`) as the fifth value, and it is the only one discharged by **certification**: the completion predicate gains a `'service'` requirement measured on `pac_certificate_items` joined to recorded `pac_certificates`, so an AMC item is executed by certified quantity and nothing is ever delivered or installed against it. Certification caps at the **sanctioned** quantity rather than the installed total — every other item's ceiling is what was installed, which for an AMC item would be zero — enforced in `routes/pac.ts` and again by a database backstop, trigger `pac_certificate_items_certified_ceiling_guard` on `app_private.guard_pac_certified_ceiling`. The review raised ten numbered findings plus three minors: **nine of the ten are code fixes**, finding 9 was an error in the PR's own description and is retracted rather than fixed, and one minor (screenshots) is explicitly declined rather than quietly dropped. The substantive four: the **out-of-AMC escape** (the category guard was asymmetric — movement blocked an item entering AMC, but nothing blocked an item with certification leaving it; now `ITEM_HAS_CERTIFICATION`, the mirror of `ITEM_HAS_MOVEMENT`); the **DC/category write skew**, closed by sorting and locking the wanted item ids at draft time _and_ re-checking deliverability under lock at issue, which is what makes "no issued challan quantity ever stands against an AMC item" true rather than merely usual; **recategorisation on a completed Work**, refused by `assertWorkOperable` before the item lock; and the certification cap's DB backstop above. Then post-rebase CI caught what local runs had not: the AMC `certified` CTE measured **1.978×** against P17's freshly recalibrated 2× ratchet. Rather than move the ratchet a second time, **the certified read was taken out of the Measurement Book loader entirely** (`133f520`) into `loadAmcCertified`, gated on a finalized book that actually holds an AMC item, so `ITEM_INPUTS_SQL` is six laterals and deliberately still six. The measured statement is byte-identical to P11's as a **git** fact — that region of `query-aggregates.integration.test.ts` carries zero P16 lines on the merged tree — rather than as an assertion; what the restored test asserts is that the current and retired statements return identical rows. A money guard rides on the moved wiring: a third fixture Work bills its AMC item on the certified quantity, `deltaPac`/`deltaFinalBill` `2.000` and `amountPac`/`amountFinalBill` `1200.00`/`800.00` against `0.00` supply and installation. Migration 0068 was normalised to LF in the same commit — 317 CR bytes removed, 14,042 → 13,725, every other migration in the series uses LF, and a migration's bytes are its checksum. One piece of drift, found by this close-out and **fixed in the same pull request as this record**: `apps/server/src/mb-remark.ts` still cited `ITEM_INPUTS_SQL`'s `certified` CTE in a doc comment after the move deleted it. The comment now points at `loadAmcCertified`/`computeForBook` and keeps the reason the read lives outside the loader.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| P14  | #68 | The row's "new inbound document kind on the `loa_documents` machinery" did not survive contact. The row lives in its **own table**, `received_railway_bills` (0066): 0040's source-shape CHECK requires every non-`'loa'` kind to hang off a parent LOA row _and_ carry a matched tender identity, and a received bill has neither — it arrives long after award, it is not tender evidence, and a Work that reached the product by import has no parent LOA row at all. 0058 had already made this exact call for variation orders and written the argument down: reuse the **machinery**, not the table. So `consumeUpload`'s magic-byte gate, the ClamAV scan, the Poppler-only extraction, the `ObjectStorage` boundary, `verifyUploadedPdf`, 0060's append-once verdict guard and the RLS and audit posture are all reused verbatim, and only the row lives elsewhere. **The three-signature verdict was strengthened, then ratified.** The review found the rule cardinality-only — three signatures could be three from one certificate — so it became a **distinct-signer** rule comparing issuer + serial rather than printed name, with its own function, refusal code and tests; the owner ratified it on 2026-08-14, which strengthens the 2026-08-13 gating rulings and leaves the rest of them intact (integrity, reaching a configured anchor, the last signature covering the file, expiry ignored). `docs/PRODUCT.md` §5.5 states it as the ruling. Ten review findings, all addressed. The severe one was a **close/discard write skew**: close locked only the book and read the bill, discard locked only the bill and read the book, and under READ COMMITTED both could commit, leaving a permanently closed measurement resting on a discarded bill. Fixed at three ends — both paths take both locks, book first; discard re-reads the closure under that lock; the record route refuses a book whose `closed_at` is set, in the pre-check and again under the row lock — and guarded by a concurrency test firing both requests in **both** arrival orders. Also: a **born-paid** hole, because the paid guard was UPDATE-only and a bill row may legitimately be created in any of its three states, now `BEFORE INSERT OR UPDATE`; the **shape-only DB closure gate** extended to validate that the closing bill exists, belongs to this organisation, settles _this_ book, is not discarded, carries a settleable stored verdict and holds ≥3 signatures — with the per-signature rule deliberately left in **one** language and the migration header and §5.5 rewritten to state that split rather than claim "enforced twice", because two copies of a subtle rule drift apart; and a **nil-amount** bill that used to die on the column CHECK after its bytes were already stored, now refused at parse as a 400 naming the field. **The L2→FL2 normalisation this row asked for was demoted to a test helper.** Wiring the reviewer's preferred cross-book duplicate check found it unreachable — a Work's finalized Measurement Books are unique per measurement sequence, and a bill is tied to its Work by the letter number it prints — so `canonicalMeasurementNumber`/`sameMeasurement` moved to `apps/server/test/helpers/measurement-number.ts`, production links by letter number + sequence with the ledger token handled **by construction**, and a test records why the duplicate case cannot arise. Defensive code no input can enter is not belt-and-braces. **P17's InitPlan census had to be restated when this pack landed on top of it** — the first composition failure of the wave. The census derived that before 0069 every policy naming a helper names it in bare position: true when written, false the moment 0066 arrived, because ADR-0010 asks new policies to be authored wrapped and 0066's was. On the staged pre-0069 database it found 64 bare against 65 naming a helper, and the missing one was P14's. The guard now states the real invariant — the bare population is the helper population **minus** the policies already in the target shape, and that difference is named — so a broken matcher still pushes policies into it and nothing changes shape without a pack saying so. Finally, the **first tracked binaries in the repository**: four review screenshots under `docs/screenshots/p14-railway-bill/`, whose README says plainly that they are review artefacts the owner may drop. |

Side-fix: **#69**, the loading-anchor census, which closes §2.7's process
note. It landed as its own pull request between #66 and #67, not inside a
pack — #68's body credits it to P17, and that is the one attribution in
the tranche worth correcting.

Process notes for tranche B:

- **A combined local `pnpm verify` is effectively unattainable during a
  multi-session wave, and that is now a measurement rather than an
  impression.** Six attempts across the tranche's sessions, each failing
  somewhere different and never in a file the pack under test had
  written: fifteen web files that could not fork at all, a
  `findByRole('navigation')` timeout, an FK-orphan census tripping on
  another suite's mid-flight cleanup in the database all five workspace
  projects share. This close-out's own combined run adds a seventh, and
  it failed the same way — three 5000 ms jsdom timeouts in
  `review-loa-departure.test.tsx`, which then passed, while a different
  file (`a11y-invariants.test.ts`) timed out in its place on the next
  run and passed on the one after. `pnpm -r` also fails fast, so
  `apps/server` never reported at all in the combined run: the expensive
  suite is the one a combined run is least likely to tell you about.
  **The standard is per-package runs against a freshly created database,
  plus CI**, and a landing record should say so rather than imply a green
  combined run that nobody has.
- The loading-state race class got its standing guard mid-wave (#69,
  `apps/web/test/views/loading-anchor-census.test.ts`). Both packs' new
  view tests pass it, which is the cheapest possible confirmation that
  the convention holds for tests written after it.
- **The time-window suites are the known contention flakes**:
  `credit-notes` (the 24-hour IRN cancellation window) and `ops`
  (login-lockout expiry). Both were seen once each under full-suite load
  in this tranche and pass alone. Record them; do not retry them into
  silence. The `apps/web` jsdom 5000 ms timeouts belong to the same
  class, but they move between files rather than sitting on one.
- **CI is authoritative, and main is never tested as main.** `ci.yml`'s
  `verify` job runs on `pull_request` (with `paths-ignore` for `docs/**`
  and `**.md`), plus dispatch and the Monday schedule; the per-merge push
  run was deliberately removed to conserve Actions minutes. So what CI
  proves is a pull request's head, never the merge commit that lands, and
  never a docs-only change at all. Composition failures between two packs
  are therefore found by the second pack rebasing, not by CI — which is
  exactly how P17's census guard and P16's ratchet were caught. Treat the
  standing caveat as a reason to rebase early, not as a reason to trust a
  green tick more than it deserves.

### 2.9 Wave 3 landing record — tranche B (2026-08-14)

The last three product packs and two side pull requests, merged in this
order: **#73** (the railway-bill timeline entities) → **#71** (P19,
migration 0071) → **#72** (P15, migration 0067) → **#74** (the
reply-inside-transaction census) → **#75** (P18, migration 0072,
ADR-0011). **Wave 3 closes here, and with it the programme's three
waves.** Every one of the three packs required fresh human review under
`CONTRIBUTING.md` — P19 touches migrations, authorisation, RLS, the
soft-deletion of a confirmed contract record and numbering-adjacent
identity constraints; P15 a migration, money, and an authority-gated
write path on financial records; P18 six of the listed categories at
once, including the first table in the repository with no grant to
`auto_mb_app` — and each was opened rather than merged, then merged
after that review.

The verification standard is tranche A's, unchanged: **per-package runs
against a freshly created database, plus CI**. No combined `pnpm verify`
is claimed for the tranche.

| Pack | PR  | Notes beyond the brief                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ---- | --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P19  | #71 | The remedy 0063's own header prescribes is executable: a supersede request against a confirmed Work, decided through the existing approval engine (`entity_type = 'work_supersede'`), withdraws the Work and returns its letter to `review` in one transaction. It is the **first writer of `works.deleted_at`** — a column every read has honoured since 0001 and nothing had ever set — and it makes the two Work identity `UNIQUE` constraints **partial over live rows**, which changes a stated product invariant from "unique forever within an organisation" to "unique among an organisation's live Works". The review raised **ten confirmed findings, two severe, and all ten are fixed.** The first severe one is worth the detail: successor identity is now enforced in **three layers** — `assertSuccessorIdentity` refuses a successor whose work code or letter number differs from the predecessor's (`409 SUCCESSOR_IDENTITY_MISMATCH`); the freed identity is **reserved** while the supersession is open, at the route (`409 WORK_IDENTITY_RESERVED`) and again by a **deferred constraint trigger** on `works` INSERT, deferred because the binding is written after the row and only COMMIT can answer "is this the successor?"; and confirming a released letter calls `assertWorkAccess` against the predecessor, which is exactly how a scoped office member had got through. Without those three, superseding is a work-code rename that no approval covers. The second severe finding was an **instrument-route TOCTOU**: `POST /api/works/:id/instruments` read the Work and inserted against it in two statements, so `for update of w` — the lock every sibling child-creating route already took — was added, with a source assertion beside the race test because a race test cannot force the interleaving. The downstream-document census became **transitive**, a recursive CTE over every table reaching `works`, which surfaced 13 more tables that are now classified with reasons; seventeen registers block and fourteen children are exempt, and the server list, the migration's SQL census and the catalog are proved equal to each other. **The owner ratified every flagged reading on 2026-08-14** and ruled the provenance fork: **option A**, one-directional provenance on the discard-and-re-upload path, stated in `docs/PRODUCT.md` §5.6, warned in the supersede panel _before_ the operator commits to a discard, and closed on the row by `released_letter_discarded_at` so the supersession reads as ended rather than perpetually waiting. **Option B — a bind-to-replacement-document step — was deliberately not built**: naming which document a Work's provenance points at is a new authoritative action, and it is commissioned only on operator demand. Two smaller things to carry: the first draft of the reserved-identity guard read `loa_documents` at COMMIT of every `works` INSERT, which made every Work creation in the organisation able to block on any lock held on that table (the export-consistency proof, which parks the export on exactly that lock, hung on it); and P17's RLS InitPlan census was changed from asserting **equality** between the pre- and post-0069 policy populations to asserting **containment**, which is the property the 0069 rewrite actually owns.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| P15  | #72 | "Paid" stops being a word with no amount behind it: `bill_payments` (one row per credit, required-reason void, no edit or delete path), `bill_payment_deductions` (typed heads — GST TDS §51, income-tax TDS §194C, retention/SD, penalty, and `OTHER` which cannot be recorded without a description) and the `bill_settlement_positions` view. The register's whole point is the three-figure position — `outstanding = what the railway settled − received − deducted` — because **money the railway kept is settled money and money that never arrived is outstanding money**, and a register reporting one net figure reports every bill as short by its own statutory deductions forever. The settlement reference is the **railway's** bill amount reached through the Measurement Book it closed, not the prepared amount: a bank credit and the railway's figure are both GST-inclusive while `bills.total_amount` is on the Work's recorded basis. **Ten review findings, all fixed** — and the fixes uncovered **three latent defects nobody was looking for**. (1) `bill_payments_void_shape_check` tested the reason's length with no `IS NOT NULL`, and a CHECK that evaluates to NULL passes, so a void with **no reason at all** — the one row the constraint exists to refuse — was accepted; the failing-path test caught it on its first run. (2) The paid guard called `bill_settlement_reference(NEW.id)`, which reads `bills`; on an INSERT the row is not in `bills` yet, so a bill whose measurement the railway settled weeks ago was refused with _"no railway bill has settled its measurement"_ — found **only** because an earlier finding pushed the tests from message regexes to SQLSTATEs, which could finally tell the two refusals apart. (3) `coalesce(d.total, 0)` falls back to an **integer** literal, so a receipt with no deductions reported `deductionTotal: "0"` where every other figure said `0.00`. A fourth defect surfaced in the re-run and was too general to keep: the record route called `reply.send()` **inside** the transaction callback, so a 201 could reach the client while the COMMIT was still in flight and a screen refetching on success could render without the receipt it had just recorded — which is how an operator records a payment twice. That one became the standing guard in **PR #74**. **The owner ratified two rulings on 2026-08-14**: exact settlement on `paid` (a bill moves to `paid` only when receipts + deductions reach the railway's figure exactly — the manual act stays manual, but the register now checks it), and the goal-seek posture as **accept-with-audit**. The goal-seek is that a holder of the `issue` authority can settle a bill with nothing received and one deduction for the whole outstanding amount; it is accepted because a bill wholly consumed by a recovery is a real event and every step names its author, with §5.7 stating plainly that **a deduction is an operator's assertion, not evidence** — the register is a working note beside GSTR-7A and Form 26AS, never proof of a TDS credit — and the ruling is to be revisited at the first real GSTR-7A reconciliation. Two things recorded rather than fixed: `bill_unpaid` on the dashboard now conflates a bill nothing has been received against with one 95% settled, and retention is recorded but never released (§5.7 previously implied a release path this product has no code for; the sentence is gone).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| P18  | #75 | The upload of a Letter of Acceptance now answers as soon as the letter is **accepted** — bytes proven to be a PDF, scanned, stored, row written `Pending` — and the reading happens in `apps/worker`, which enters the deployed set in the same pull request that gives it a real job, discharging ADR-0008's tripwire. **Two of the row's four named operations moved, not four**: Poppler extraction and signature verification, sharing one job on one storage read. **ClamAV stays synchronous, ratified by the owner**, because it is an admission gate rather than post-processing — today nothing unscanned is ever _written to storage_, and an async scan could only hold the strictly weaker promise that nothing unscanned is ever _served_, across every download, render, export and PDF path. **The Gotenberg render vertical is deferred with its design recorded**, not skipped: seven persisting render routes each build their HTML in their own module, so the shape that does not violate ADR-0011 §3 is for the request path to write the built HTML to object storage and enqueue a **reference**; it is a second vertical of comparable size and is commissioned later on owner appetite. The queue is unreachable from the application role — `worker_jobs` is the repository's first table with **no grant to `auto_mb_app`**, FORCE RLS and no policy, reached only through definer functions — and a job runs as the user who caused it, `enqueue_job` taking no organisation and no user because it reads both from the binding already active. The review raised **ten findings, all fixed**, and **nine of them clustered on one thing: the queue knew when a job was over and nothing else did.** Four fixes carry the weight: `processing` is now **resumable**, with each write's idempotency stated at its call site and the audit INSERT made conditional on the extraction UPDATE's row count because it is the one write that is not naturally idempotent; an **attempts-budget reconciliation** arm in `claim_next_job` parks over-budget expired claims as `failed`, which must live there because a crashed worker cannot park itself, and a merely-unclaimable row is invisible; `reconcile_terminal_job` moves a document stranded by a terminal job to `failed` **in SQL**, because the worker is exactly what cannot be relied on at that moment; and `release_job` hands a job back for **handler skew** — an unknown kind during a rolling deploy used to burn the retry budget and terminally kill a good job in about 2.5 minutes — returning the attempt with a 30-second hold. **Two ADR-0011 amendments were ratified by the owner on 2026-08-14.** (a) The **residual-exposure bound was falsifiable and false**: `claim_next_job()` hands the claim token to whoever called it, so arbitrary SQL holding the application role can **claim and destroy** jobs — `complete_job` marks work done that never ran, and `fail_job(…, 'refused_bind')` forges a membership-revocation signal an operator would read as a real access change — which is not bounded by the lease, because a completed job never returns. It remains _not_ a tenancy break, and finding 3 reduces the blast to a visibly `failed` document with a remedy. (b) "P18 moves **four** operations off the request path" was wrong by two, which matters because a reader taking it at face value would believe upload scanning had become asynchronous. **The definer grant widening was accepted**: `auto_mb_definer` gains `SELECT, UPDATE` on `loa_documents` (no INSERT, no DELETE — reconciliation moves an existing row) and `DELETE` on `worker_jobs` for the purge; the role is NOLOGIN and reachable only through definer functions, so this widens what those functions may do rather than what any session may do, and both grants are repaired in `bootstrap.ts`. Screenshots were **substituted rather than dropped** — the three changed states are text-only, pinned by `loa-pending-states.test.tsx`, which runs in CI on every commit as a PNG cannot. `packages/documents` is a new workspace package holding the storage, extraction and signature modules moved **verbatim**, because the alternative was a second copy of a subtle security rule inside the worker. One piece of drift, found by this close-out and **fixed in the same pull request as this record**: a day after the owner ratified them, `adr/0011-worker-tenant-binding.md` still carried both amendments as _proposed_ and _awaiting owner ratification_, in its status line and in its own heading. An accepted decision record that reads as pending is the failure mode §2.8 found twice over; the ADR now states the ratification, and nothing else in it moved. |

**Side pull requests, both from chip sessions rather than packs.** **#73**
put the received railway bill on the Work's timeline: recording,
discarding and closing a Measurement Book against one wrote audit events
that no operator-reachable screen ever showed. It also added the
`audit-timeline-census` standing check, which surfaced **seven**
pre-existing Work-linked entity types that have never been on the
timeline (`loa_documents`, `extension_requests`, `purchase_orders`,
`budgetary_quotations`, `tax_invoices`, `credit_notes`, `eway_bills`) —
documented as known gaps rather than endorsed or fixed. **#74** is the
standing census for P15's reply-inside-transaction race: a lexical scan
of `apps/server/src` for any use of `reply` inside a transaction
callback, held against a frozen allowlist exact in both directions. The
whole tree was swept, and beyond P15's own fix exactly one site holds the
reply inside a transaction — the streamed full-organisation export, whose
REPEATABLE READ snapshot must stay open while the client consumes the
stream — which is the allowlist's single entry with that reason written
next to it.

Process notes for future waves:

- **Identical-value bumps to a shared counter census merge silently and
  wrongly, and this bit twice in one tranche.** The trigger census in
  `migration-contract.test.ts` was raised 146 → 150 by both P15 and P19
  independently, for four triggers each; because the two edits were
  textually identical, git resolved them to `150` with no conflict and no
  warning, when the merged series had eight new triggers. Re-derived
  against the merged tree as **154**. Then P18 raised it 146 → 148 on a
  branch while `main` stood at 154, and re-derived again as **156** —
  neither branch's number was right. A three-way merge cannot flag an
  identical-value edit to a counter, so the rule is to **re-derive by
  grep after every merge** rather than trust what came through:
  `grep -ciE '^[[:space:]]*create trigger ' packages/db/migrations/*.sql`.
- **A bootstrapped database and a migrations-only database are two
  different privilege environments.** Every LOA upload on P18's branch
  answered 500 with `permission denied for function current_user_id`,
  raised from inside `enqueue_job`: the function runs as
  `auto_mb_definer` and the definer role held no EXECUTE on it. It passed
  locally because every database built there was bootstrapped, and
  `applyGrants` re-owns that function to `auto_mb_definer`, where the
  owner's implicit EXECUTE covers the call — while the server suite
  builds its database with `runMigrations` alone, which is how CI runs
  it. **Every new definer-calls-definer function needs an explicit grant,
  proven against the migrations-only shape**, rather than resting on who
  happens to own what. Add a migrations-only run to the recipe when a
  pack ships one.
- **A docs-only pull request skips the verify workflow entirely, so "no
  failing checks" is not "checks passed".** `ci.yml`'s `paths-ignore`
  covers `docs/**` and `**.md`, and a workflow skipped by `paths-ignore`
  reports **no status at all** rather than a passing one — which is why
  `census.yml` deliberately carries no path filter and is the workflow to
  require under branch protection. A records pull request like this one
  should say in its own body that verify was skipped, rather than let a
  green-looking checks list imply a run that never happened.
- **PowerShell `Out-File` writes CRLF and a BOM, and a migration's bytes
  are its checksum.** Migration 0068 had to be normalised to LF in
  tranche A — 317 CR bytes removed — because every other migration in the
  series uses LF and an applied migration is sealed by its hash. Write
  migrations with a tool that emits LF without a byte-order mark, and
  check the bytes rather than the rendering.

---

### 2.10 Wave 4 — owner observations of 2026-08-14

Commissioned from four owner observations delivered in session on
2026-08-14, with three scope decisions ratified the same day: the
installations space is both a Work tab and a top-level register; the
e-way bill takes the full scope (invoices and challan "stage 3b"
statutory facts); keyword auto-mapping leaves unmatched items
uncategorised rather than guessing. Invoice and e-way-bill PDF
generation exposed in the UI was added to scope by the owner mid-wave.

| Pack                               | Scope                                                                                                                                                                                                                                                                                               | Notes                                                                                                                                        |
| ---------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| **W4-P1 Challan carry-forward**    | From the second Delivery/Issue Challan of a Work, the standing choices of the previous document (DC: prefix + consignee snapshot; IC: issued-to/role/location) are the new draft's defaults. Movement type is never carried.                                                                        | Review moved the source of truth server-side: the balance payload carries the last **issued** challan's standing fields, chosen by sequence. |
| **W4-P2 Installations space**      | Installation records leave the Deliveries tab: own Work tab (with the serial trace) plus a top-level cross-work register with a new tenant-wide, work-scoped list route.                                                                                                                            | The new list route touches work-scope authorisation — fresh human review required.                                                           |
| **W4-P3 Payment-matrix prompt**    | After LOA confirm, a one-time popup collects the payment matrix and proposes item categories from description keywords (supply / supply+installation / installation trade words). Proposals never persist unconfirmed; unmatched items stay uncategorised; SPARE_SUPPLY and AMC are never proposed. | Evolves the spec §8 rule: extraction still never proposes; the popup proposes, the reviewer confirms.                                        |
| **W4-P4 Direct-invoice workspace** | Org-wide tax-invoice module UI over the existing direct-invoice server path (private customers), including render/PDF surfaced in the UI.                                                                                                                                                           | **Landed.** The server had no tenant-wide invoice list, so the pack added `GET /api/tax-invoices` and migration 0074's register index. Scope posture copies the standalone Delivery Challan: a direct invoice takes organisation-wide reach, and the writer check was tightened to agree with the register. Money + issued documents + a new work-scoped route + a migration — fresh human review required. |
| **W4-P5 E-way bill module**        | Goods/HSN facts (challan stage 3b), NIC payload builder re-authored, SAC refusal narrowed to line content, EWB printable PDF, UI. Supersedes the blanket generation refusal from the 2026-08-10 audit disposition finding 1 — by owner decision.                                                    | Largest pack; design recorded before code. Linkage change: EWB raisable from a goods invoice or a standalone challan.                        |

---

## 3. Standing corrections

1. ~~`docs/OPERATIONS.md` and `docs/RUNBOOK.md` must state only what
   `scripts/backup.sh` actually does until P6 lands (encryption, retention,
   off-host copy are currently claimed and absent). P6 owns this edit.~~
   **Done in P6.** `scripts/backup.sh` now performs envelope encryption,
   carries `AUTH_SECRET` in the recovery set, establishes dump/archive
   consistency by ordering, and gates the freshness marker on a configured
   off-host copy; `docs/OPERATIONS.md` §5 and `docs/RUNBOOK.md` §4/§4a/§5
   describe exactly that and name what is still absent — no point-in-time
   recovery, no pruning in the script, no object-storage versioning, and no
   chosen off-host destination.
2. `AUDIT-2026-08-12.md` and the disposition carry three overstatements
   (constraint-trigger wording; "malware scanning fails closed" without the
   config caveat; "every membership read" while two writes lack the predicate)
   and no as-of commit marker. Any future citation should verify against code.
3. The independent review's factual errors (§1.2) are corrected here so they
   are not re-imported.
4. §1.3 row 21 says "5 of 11 counter tables lack the decrease guard"; P2
   measured 4 of 11. The fifth candidate, `extension_request_counters`, is
   exempt by design: migration 0029's top-of-sequence delete path decrements
   the counter to keep the extension sequence gapless, so a monotonicity
   guard would break it. The exemption is recorded in migration 0064 and
   enforced in `migration-contract.test.ts`.

_Maintained with the same rule as the audit disposition: when a pack lands,
update its row in the same PR._
