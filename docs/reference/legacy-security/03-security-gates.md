## 2. Security gates

> **Correction, 27/07/2026.** The previous version of this section listed
> eight gates as *"CI security gates (build-failing, live from Stage 0)"*.
> That was an overclaim on two independent counts, both re-measured for this
> edit rather than inferred:
>
> 1. **The CI runner arrived after this correction was written.**
>    `.github/workflows/ci.yml` (48 lines) now runs `pnpm gates` on GitHub's
>    infrastructure for every push and pull request. It is still not
>    build-failing in the sense an auditor means, because the `gates` check
>    is not a *required* status check — it reports, it does not block a
>    merge. See the CI-runner row in §2.2 for the outstanding owner action.
> 2. **Four of the eight named tools are absent**, and two more name
>    ESLint rules that do not exist in `eslint.config.js`.
>
> As of PR #3 (`059771d`) `origin/main` carries `package.json`,
> `eslint.config.js` and `.secretlintrc.json`, so §2.1's gates are on the
> default branch, not only on a feature branch. This correction records that
> the original claim was inflated, not that coverage was removed.

### 2.1 Wired today — the `pnpm gates` chain (verified green 27/07/2026)

| Gate | Tool | Proves | Invoked by |
|---|---|---|---|
| Lint | ESLint 9 (`js.configs.recommended` + `typescript-eslint` recommended) | Baseline JS/TS correctness. Since [SEC-4] the config also carries the kernel-boundary and frontend-ban rules — their own rows below | `pnpm lint` |
| Kernel boundary (import half) | ESLint `no-restricted-imports` in `eslint.config.js` ([SEC-4], `decisions/APPROVED-030`) | Everywhere outside `packages/kernel/**`, in **static `import`/`export` declarations**: no deep import past the kernel public entry (`@auto-mb/kernel/*` subpaths), no relative import resolving into `packages/kernel/`, no cloud SDKs (`@aws-sdk/*`, bare `aws-sdk`, `@azure/*` — ARCHITECTURE.md §10 discipline 3). `packages/kernel/**` itself is exempt (it may import its own internals and holds the SDKs). **Widened 29/07/2026 [SEC-7]** (`decisions/APPROVED-037` Option A, closing F6; round 2 after the cross-model review): the dynamic spellings are banned too — `import()` / `await import()` and `require()` of the same three target sets, where coverage is the full spelling product: require callee ∈ {bare `require`, `X.require`, `X["require"]`, `` X[`require`] ``} × argument ∈ {string literal, expression-free template literal} — the review measured `module.require`/`globalThis.require` and an aliased `require` slipping past the bare-callee-only first cut with plain literal arguments, inside the claimed class. **Aliasing `require` itself (`const r = require`) is now banned at the alias's creation** — receiver-scoped exactly as the document-alias cut is (round 3): the member arms are limited to the loader globals (`module`/`globalThis`/`global`/`self`/`window`), so a domain `.require` member — `pkg.exports["."]["require"]` is a standard package.json exports condition this workspace's own tests read — stays lawful, probe-asserted. `require.call`/`apply`/`bind` indirection is banned wholesale (round 3; no lawful product-code use). The boundary block's extension list gained `jsx`/`mts`/`cts` (a `.mts` product file was invisible to every block; `.jsx` had the sink bans but not the boundary — both review-measured). Each class carries a failing-fixture probe case asserting the selector's **own message**, not just the shared ruleId (the review deleted a whole selector family and the ruleId-only probe stayed green off a sibling); a dynamic import of the public entry `@auto-mb/kernel` and a domain-object field are probe-asserted quiet. Honest residues, stated not glossed: an `import()`/`require()` whose argument is a runtime-built expression, a loader produced by `createRequire()`, and a `require` alias obtained by **destructuring** (`const { require: r } = globalThis`) are undecidable by these selectors and stay uncovered by lint — the DC-5 source scan and review law carry those classes. Complements DC-1's package.json-level workspace-layout test and the source scan owed to DC-5: lint is the fast in-editor signal, the tests are the proof. Proven discriminating by `scripts/probe-eslint-gates.mjs` (`pnpm eslint-gates:probe`) | `pnpm lint`, `pnpm eslint-gates:probe` |
| Frontend bans | ESLint `no-eval` + `no-restricted-syntax` in `eslint.config.js` ([SEC-4], `decisions/APPROVED-030`) | In `packages/**` (product code): `eval` banned; the `dangerouslySetInnerHTML` JSX attribute, **direct** `.innerHTML`/`.outerHTML` assignment (`el.innerHTML = …`) and **literal** `document.write`/`writeln` calls banned as string-built-HTML sinks (§8 row `1.2.1, 1.3.2`). **Widened 29/07/2026 [SEC-7]** (`decisions/APPROVED-037` Option A, closing F8; round 2 after the cross-model review): computed members and `insertAdjacentHTML` are banned across the full key-spelling product {`.name`, `["literal"]`, `` [`template`] ``} — the review measured template-literal keys (`` el[`innerHTML`] ``) bypassing the literal-only first cut, inside the claimed class. The `document.write`/`writeln` **call** receiver set is deliberately broad (round 3): bare `document` plus **any** `.document` member in all three key spellings — `window["document"].write` defeated the round-1 cut, and round 2's global-qualified narrowing lost `frame.contentWindow.document.write()` (a textbook XSS sink with no lawful shape), both review-measured and both probe-cased now. The aliased-sink class (`const d = document; d.write(…)`, `const d = window["document"]`) is banned at the alias's **creation** — AST selectors cannot track data flow, so banning the aliasing of the GLOBAL document is the sound selector-level cut; the ban is deliberately scoped off `window`/`globalThis`/`self` so a **domain `document` field** (PRODUCT-SPEC PBG "amount, submitted amount, expiry, document" — `dto.document = row.document`) stays lawful, probe-asserted, and a `.write()` on a non-document receiver stays quiet (streams/responses do not trip it). **Corrected and closed 02/08/2026 [SEC-14]** (`decisions/APPROVED-073`, re-audit 2026-08-02 H3/H4/MED): the [SEC-7] claim that enabling `no-new-func` gave `new Function` a lint layer was **measured false for every spelling but the bare literal** — `no-new-func` is scope-based, so `const F = Function; F(src)` and `globalThis.Function(src)` evaded it AND the semgrep rule (which matches literal `new Function` only), leaving CWE-95 string-built code execution with zero automated cover for those spellings. The Function constructor is now selector-banned under the SEC-7 alias-at-creation doctrine, **hardened by the in-PR cross-model review (opus, REQUEST-CHANGES → all findings fixed and fixture-pinned in the same PR)**: alias **creation** from the bare global or a `globalThis`/`global`/`self`/`window` member in all three key spellings AND through **optional chaining** (`globalThis?.Function` — the review measured the `ChainExpression` wrapper defeating every plain member arm, review F2), plus the other decidable binding positions (`{F: Function}`, default parameters, class fields — review F7); **call/new on any receiver** (the SEC-7 round-3 broad-receiver rule applied symmetrically: `frame.contentWindow.Function(src)` is the constructor from another realm with no lawful shape **in this workspace's product code** — accepted FPs from the breadth are recorded below with their rewrites; only the ALIAS arms stay global-scoped, so a domain `.Function` field read stays lawful, probe control; review F3); and **indirect invocation** — `Function.call/apply/bind` bare and member-spelled, `Reflect.construct/apply(Function, …)` — mirroring the SEC-7 `require.call` arm (review F4). Closed with it: `dangerouslySetInnerHTML` via a **JSX spread**, descendant-matched so a conditional/nested/cast-wrapped source object literal is still seen, key spellings covering identifier, string literal AND expression-free template literal (reviews F1/F9); and the reflective HTML writes with honest sink semantics (review F5, measured against a prototype-setter harness): **`Reflect.set(el,"innerHTML",h)` invokes the setter** — the true assignment twin; **`{Object,Reflect}.defineProperty` is a *shadowing* write** ([[DefineOwnProperty]] does not consult the prototype chain, so it parses no HTML) banned as defense-in-depth, not claimed to be the sink; and **`Object.assign(el, {innerHTML: h})` — setter-invoking, the idiomatic spelling — is banned too**. Sink arms cover bare + one-level-qualified receivers (`globalThis.Reflect.set`), the three method-key spellings, and literal/template/TS-cast property arguments (`"innerHTML" as string`). Each spelling class carries a red-driving probe fixture asserting its own message, plus quiet controls (domain `.Function` member and `Function` type annotation, benign `Reflect.set`/`defineProperty`/`Object.assign` keys, the CJS `__esModule` idiom, variable and benign JSX spreads, docs/ exemption). Extension list gained `jsx`/`mts`/`cts` (review-measured invisibility). Each widened class carries a failing-fixture probe case asserting the selector's own message. Honest residues, stated not glossed: **aliasing** a document reached through a non-global receiver (`const d = frame.contentWindow.document; d.write(…)` — direct calls on such receivers ARE caught) and a sink keyed by a **runtime-computed expression** (`el[k] = …`) are undecidable at lint — review law and the semgrep layer carry what they can of those. The [SEC-14] classes add their own, same doctrine: a **variable** JSX spread (`{...props}` — data flow) and a variable `Object.assign` second argument, a runtime-computed key or property-name argument (`Reflect.set(el, k, h)`), `Object.defineProperties(el, {innerHTML: …})` (second-arg object walk, not a literal-arg call shape), a **destructured** `Function` (`const {Function: F} = globalThis`) and array-element references (`[Function]`) — the same disclosed destructuring residue the require arms carry — receivers qualified deeper than one member hop (`a.b.Reflect.set`), and the tokenless `(() => {}).constructor(src)` route stay undecidable-or-unbanned at lint and are carried by review law. A `SequenceExpression` alias init (`const F = (0, globalThis.Function)`) is the same wrapper class one node over, disclosed not banned (round-2 G1 N7). Review-measured accepted FPs, recorded with rewrites (the semgrep row's documented-FP discipline): a local variable shadowing a global name (`const window = {Function: "x"}; window.Function`) trips the alias ban — esquery is not scope-aware, identical to the pre-existing document/require arms; **`new lambda.Function(...)`** (the AWS CDK canonical spelling, incl. inside `packages/kernel/**` where ARCHITECTURE.md §10 puts cloud SDKs) trips the broad-receiver new arm — no CDK dependency exists today; if IaC ever lands, rename the import or move construct definitions out of the linted product tree; and a **type/validator registry object literal** (`{String, Number, Function}`) trips the binding-position alias ban — rewrite as `typeof x === "function"` checks or string tags. Owed follow-up (filed to `memory/inbox/`): the SEC-7 `document`-alias arms have the same optional-chain hole the review found here (`const d = window?.document` evades) — a SEC-7-scoped fix, not silently annexed to this ticket. `docs/` (the CEO dashboard) is deliberately out of scope — its `innerHTML` inventory is §12A's, and the probe asserts the exemption | `pnpm lint`, `pnpm eslint-gates:probe` |
| Typecheck | `tsc --noEmit` | Type errors | `pnpm typecheck` |
| Unit / integration | Vitest | **Product suites exist as of 04/08/2026** ([DC-2] `ab8a186`, [DC-22] `5072786`, [DC-3] `5d45235`) — the "none exist yet" claim this cell carried since 29/07/2026 is retired. **This cell deliberately no longer carries a per-file test inventory.** It carried one for a single day: the enumeration written on 04/08/2026 was stale by that evening (70 → 80 tests, one file 21 → 31) and the figure it replaced had been stale since OPS-18 `6a0836c` added workspace-layout case (h). A count no gate checks and every ticket invalidates reads to an auditor as a measurement while behaving as decoration, so what this cell records instead is the **collection rule and its blind spot**, which a reader can re-derive at any commit: root `vitest.config.ts` collects `packages/**/*.{test,spec}.{js,ts}` and nothing else — so (i) any package's plain unit/integration suite joins the chain the moment the file is added, with no registration step to forget, and (ii) **`*.dbtest.ts` files are structurally invisible to this gate**, their suffix not being `.test`/`.spec`. Dated snapshot, not a maintained figure — re-measured 05/08/2026 after wave 6 ([DC-24] `713a7e7`, [DC-26] `6efc465`): **11 files / 260 tests** in-chain, against **13 files / 254** in `packages/db`, **5 / 178** in `packages/loa` and **3 / 38** in `packages/works`. **The durable rule keeps being exercised in both directions, which is why the snapshot is kept and the per-file inventory is not:** `packages/loa` is now the largest suite in the repo and **every one of its 178 tests is in-chain** — it has no `.dbtest.ts` at all, because a pure library needs no cluster — while `packages/db` and `packages/works` keep adding tests the glob never sees. **Eleven `.dbtest.ts` files across two packages hold 218 tests no gate runs** (200 of the db package's 254, plus 18 of works' 38); the chain sees 260, and the arithmetic closes: 8 config + 54 db-plain + 178 loa + 20 works-plain = 260. The old bar — *no §3–§12 row may cite it until product code exists* — is released only in form: a row cites a **named test file**, never this aggregate step, and what the chain covers is the pure/unit half of `packages/db` plus `packages/loa`'s manifest/purity suite, not product behaviour behind an endpoint. **Stated bound — the DB-backed suites are NOT in this chain, and they now carry the load-bearing proofs:** `packages/db/test/*.dbtest.ts` holds the live-cluster proofs of the runner's out-of-order / checksum / transaction-mode refusals, the CVE-2025-8713 version floor, [DC-3]'s catalog-driven schema conventions (`schema-conventions.dbtest.ts` — itself DC-3's `verify:` line, so that ticket's acceptance evidence is *not* gate-run) the UTC partition-bound regression (`audit-log-partition-tz.dbtest.ts`), and — since `65c2a24` — [DC-4]'s catalog-driven RLS coverage (`rls-coverage.dbtest.ts`, 21 tests, the sole proof that tenant isolation is enabled at all) and [DC-45]'s units master (`units-master.dbtest.ts`, 57). Both are their tickets' `verify:` lines, so acceptance evidence keeps landing outside the merge gate — six tickets running as of wave 5, which now adds [DC-15]'s permanent work-code uniqueness (`work-code-uniqueness.dbtest.ts`, including the partial-index-would-be-wrong demonstration) and its service suite in a SECOND package (`packages/works/test/work-code-service.dbtest.ts`). None is matched by the root `include` glob; they run only under `pnpm --filter @auto-mb/db test` / `test:db` and now `pnpm --filter @auto-mb/works test` / `test:db`, which no gate invokes. **Second-order cost, recorded where the harness bound lives ([DC-15]):** because the DC-2 cluster harness is `packages/db`'s own test-only helper and `workspace-layout.test.ts` case (e) bans cross-package subpath imports, `packages/works` carries a 302-line COPY of `pg-cluster.ts` rather than importing it — a deliberate, documented rule-(e) casualty (its header says so and names the amendment that would retire it), not drift. It is a duplicate today and a divergence risk tomorrow: a fix to one harness does not reach the other, and no gate compares them. Any future row citing a `.dbtest.ts` file is citing a test **`pnpm gates` never runs** and must say so in its Proving-test cell. **Stated bound — this gate can go vacuously green (audit M-13):** root `vitest.config.ts` sets `passWithNoTests: true` with `include: packages/**/*.{test,spec}.{js,ts}`, so if that glob ever stops matching (a file moved or renamed, or the pattern edited) the step reports success over **zero** tests — measured, not inferred: a non-matching filter prints `No test files found, exiting with code 0` and exits 0. The suite's own guard against exactly this hazard, case (g) *"no `packages/*/vitest.config.*` sets `passWithNoTests`"*, iterates `listPackageDirs(PACKAGES_ROOT)` and therefore never inspects the **root** config — the one that actually sets the flag. The guard was written against the children while the flag lives in the parent. Mitigating and measured: the per-package path is strict — `npx vitest run` in `packages/web` prints `No test files found, exiting with code 1` and exits **1**, because each `packages/*/vitest.config.ts` deliberately omits `passWithNoTests` and carries a comment naming this false-green class. **Owed (audit M-13 follow-up, not yet ticketed):** case (g) must be extended to inspect the **root** `vitest.config.ts` — not only `packages/*` — so the false-green flag is caught where it actually lives; until then the root flag is unguarded and the per-package strictness above is the only backstop | `pnpm test` |
| Secrets | secretlint (`preset-recommend` + a `github_pat_[A-Za-z0-9_]{22,}` pattern rule) | No credentials in the repo, **including the fine-grained PAT format `docs/README.md` instructs the CEO to mint** — the format the preset alone did not catch. **Both of this gate's config surfaces are frozen paths:** `.secretlintrc.json` (the ruleset) and `.secretlintignore` (the scope control — an agent-writable ignore file silences the gate exactly as a rewritten ruleset would), each cased in `is_protected_path` (`.claude/hooks/frozen-paths.lib.sh`); `.secretlintignore` since `decisions/APPROVED-021` (`df7221f`). Measured 29/07/2026 with `CLAUDE_TICKET` unset: an Edit payload against either exits **2**. Named here because `.secretlintignore` was frozen in the hook and in `guard.sh`'s header comment but appeared in **no** documentation surface at all (audit N-1) | `pnpm secrets`, `.secretlintrc.json`, `.secretlintignore` |
| Guardrail hooks | `scripts/test-hooks.sh` | **948 cases** over `.claude/hooks/guard.sh`, `security-md-check.sh` and (new, `decisions/APPROVED-021`) `frozen-effect-check.sh` — 291 through `decisions/APPROVED-021`, plus 20 header-block-binding cases under `decisions/APPROVED-023` (the [BIND-2] MINOR added a CRLF-saved-decision case: the shared parser now trims the header CR on decisions too, proven to grant exactly as its LF twin), plus 12 ticket-list-binding cases under `decisions/APPROVED-027`, plus 6 per-commit `--ci` attribution cases under `decisions/APPROVED-025` [DC-46], plus 15 gate-script freeze cases under `decisions/APPROVED-029` [PENDING-029], plus **10 from the [DC-46] post-review hotfix** (`f9f1825`) — 3 merge-carried `--ci` backstop cases (evil merge, take-ours, and the merge-that-names-its-own-covering-ticket false-positive guard) and 7 extending the `APPROVED-029` freeze class to `.semgrep.yml` and the three SEC-4/5/6 probe scripts, plus **20 under `decisions/APPROVED-049`** [GATE-2] freezing the four GATE-1 scripts (4 unauthorized-Edit + 4 bound-in-Scope-grant + 4 bound-wrong-Scope-block + 2 Bash-redirect over the checker/prover pair + 6 in `GATE1_REPO`, a fixture whose ONLY protected path is a GATE-1 script so the repo-wide `repo_has_protected_paths` site discriminates on its own — see the freeze note in the Dashboard + memory provers row). plus **5 under `decisions/APPROVED-048`** [SEC-10] pinning the retired stale `Authorizes:` tokens (audit H-1) — `SEC-4` and `SEC-12` each blocked against both `.claude/hooks/guard.sh` and `scripts/test-hooks.sh`, plus the false-positive guard that `APPROVED-013` itself still ALLOWS, which is what makes that change a token retirement rather than a revocation of the decision's own grant. These are deliberately **real-repo** cases: the defect lived in the real `APPROVED-013` header, so no fixture can pin the repair. Plus **9 under `decisions/APPROVED-052`** [SEC-11] retiring the last two over-broad `Authorizes:` grants (audit H-5 and its `APPROVED-046` sibling) — `OPS-14` and `MIG-2` blocked against `.github/workflows/ci.yml` and the frozen `portability-check.sh`/`probe-portability-check.sh` pair, each paired with the legitimate grant it must not touch (`APPROVED-046`, `SEC-6`, `MIG-1`, and `MIG-2`'s own `package.json` job) plus an unbound control. Plus **32 under `decisions/APPROVED-053`** [SEC-13] retiring the ten legacy `PENDING-0nn` self-tokens (audit L-6) — 14 BLOCK/ALLOW pairs over the seven that measured as **live** grants (016, 018, 021, 022, 023, 027, 029) against `package.json`, `.claude/hooks/frozen-paths.lib.sh`, `frozen-effect-check.sh`, `guard.sh`, `scripts/test-hooks.sh`, `.github/workflows/ci.yml` and `decisions/APPROVED-022-*.md`, plus 4 unbound controls; the three that measured inert (015, 019, 026) are unpinnable by construction — no path in their `Scope:` lines is protected — which the block states in place of claiming coverage it does not have. Plus **20 under `decisions/APPROVED-044`** [ORG-5] freezing the formatter gate's config pair `.prettierrc.json`/`.prettierignore` — 4 unauthorized Edit/Write + 2 Bash write-channel (including `echo '**' > .prettierignore`, the cheapest spelling of the measured bypass) + 2 bound-wrong-`Scope`-block + 2 bound-in-`Scope`-**grant** + 1 asserting that grant does not leak to `packages/kernel` + 2 over the prover `scripts/probe-format-gate.sh` + 7 in `PRETTIER_REPO`, a fixture whose ONLY protected paths are the pair so the repo-wide `repo_has_protected_paths` site discriminates on its own (4 git verbs + 1 single-path + 2 false-positive controls: `.editorconfig` and an ordinary source file stay writable). The grant half is not optional — a BLOCK-only suite is satisfied by a guard that denies everything. Plus **21 under `decisions/APPROVED-054`** [GATE-4] freezing `ROUTING.md` + wiring `routing:probe` (itemized in that §14 entry) and **15 under `decisions/APPROVED-058`** [SKILLS-2 follow-up] freezing the executed `.claude/skills/**` tree with the `SKILLS_REPO` fixture — components that landed recorded only in §14, reproducing the M-4 drift this cell has now corrected **three** times (and note the [SKILLS-2] §14/§12A text mis-states its own delta as "+56 (419 → 475)"; the endpoint is right, the delta was 15 from 460). Plus **39 under `decisions/APPROVED-059` + `decisions/APPROVED-061`** [BIND-1→BIND-2] — the binding ledger `decisions/BINDINGS.md`, REWORKED from BIND-1's 20 after the cross-model review blocked that diff (PR #109; its two BLOCKERs are now the section's anchor cases, each mutation-verified to go red without its fix): 11 kept freeze/no-op cases in `BINDINGS_REPO` (3 Edit/Write/redirect + 4 repo-wide git verbs + 2 false-positive + 2 empty-ledger env controls); 4 trust-root denials — committed-but-UNSIGNED ledger grants NOTHING via Edit and Bash (BLOCKER-1 close; these two GRANTED pre-fix), wrong-key and wrong-author signed commits deny; 8 verified-grant mechanism cases against pin-spliced hook copies signed by a fixture forge key (grant with env unset via Edit + Bash, WHO-not-WHAT, union, ambiguity, dirty-read, wrong-Scope, no-decision); 3 folded-in-MINOR cases (`.*` token rejected — granted pre-fix, per-token validation, CRLF-saved grant still grants); 1 populated-ledger false-positive control; 2 REAL-pin regression cases (the real dashboard commit `f4f4d58` verifies in-container against the real pin — the APPROVED-061 empirical mandate, pinned; the agent commit `d89e7a5` is rejected); 2 effect-surface hook-mode cases (unsigned committed ledger denies — BLOCKER 1 on surface 2, granted pre-fix; verified grant allows) and 5 `--ci` cases (tip grant does NOT launder an earlier unattributed frozen commit — BLOCKER-2 close, laundered pre-fix; the verified grant commit itself passes; a grant works FORWARD per-commit; an unsigned agent ledger commit is an offender — `decisions/BINDINGS.md` carved out of `ci_exempt`, invisible pre-fix; ticket-attributed ledger-file maintenance stays green). Plus the second-review round (BLOCKER 3): 2 guard + 1 `--ci` cases proving an agent merge that resurrects a REVOKED ledger grant (unsigned merge, ledger blob copied from an older CEO-signed commit) grants nothing — the newest-verified-toucher-blob-equality fix, each mutation-verified to relaunder under the old `git log -1 -- <path>`. Plus the [BIND-3] round (`decisions/APPROVED-062` Option A, the `--ci` base-ref revocation anchor): 7 cases — the branch-rewind, rooted-before-the-revoke and first-push-rewind resurrections each measured resurrecting at exit 0 pre-fix and blocked at exit 2 post-fix; the honest-branch, fetch+merge-pickup and first-push controls still grant; and the hook-mode residual pinned honestly at exit 0 (local refs are agent-owned; the anchored `--ci`, a required check, is the wall). Plus **19 under `decisions/APPROVED-063` [GUARD-1]** closing the `git read-tree -u`/`checkout-index` write-channel gap (measured in the PR #109 review, `memory/inbox/2026-07-31-bind2-followups.md`): the two working-tree-overwrite attacks (`git read-tree -u --reset HEAD`, `git checkout-index -f -a`) each blocked in a protected repo, plus the `-m -u` separate-flag form, the self-`-C` form, `checkout-index --stdin`, an explicitly-named frozen path under `checkout-index --`, and — from the PR #116 review BLOCKER — `checkout-index --prefix=<frozen dir>` in both the `=` and space-separated spellings (`--prefix` redirects the write destination to `<dir>/<indexpath>`, so the argv paths no longer name where the write lands; conservative-denied, not attributed); and — from the PR #116 review MAJOR — the git long-option **abbreviation** ladder, since `checkout-index` is parse-options based and resolves any unambiguous prefix (`git checkout-index -f --a` reverted a tampered `guard.sh` at exit 0 pre-fix, and the revert leaves a clean tree so the effect wall is blind): `--a`/`--al` == `--all`, `--std`/`--stdi` == `--stdin`, `--p`…`--pref=` == `--prefix` now each matched down their whole ladder; controls that must stay ALLOWED — index-only `git read-tree HEAD` (rewrites only the index), a named NON-frozen `checkout-index` path, both verbs in a repo with no protected paths, `--prefix` in a repo with no protected paths, and `checkout-index --stage=all <named path>` (the `--stdin` abbreviation neighbor the ladder must not over-deny); and the `**`-vs-narrow-`Scope` grant discrimination the `reset --hard` block carries. Plus **5 under `decisions/APPROVED-065` [GUARD-3]** separating the `[real-repo smoke]` section from the live binding ledger: those cases pin ENV-channel invariants against the REAL decision headers (which no fixture can), but they used to probe `$REPO_ROOT` itself and so read the LIVE `decisions/BINDINGS.md` — a legitimate web-flow-signed `Bindings: GUARD-1` grant (`APPROVED-063` Scope covers `guard.sh` + `scripts/test-hooks.sh`) flipped 12 of them exit 2 → 0 while the guard behaved CORRECTLY (a granted path IS writable). The section now runs against `SMOKE_REPO` — a throwaway derivation of the real repo built by `build_ledger_neutral_smoke_repo`: real `decisions/*.md` copied verbatim, frozen-path shape seeded, HEAD committing an EMPTY `decisions/BINDINGS.md` in fresh suite-authored history, so the ledger contributes zero ids twice over (empty `Bindings:` line AND an unsigned last toucher the `APPROVED-061` trust root rejects). The 5 proving cases: 2 controls reproducing the reddening state under the BIND-2 fixture forge (fixture-signed `Bindings: GUARD-1` honored — unbound Edits of both covered files correctly ALLOWED in the granted source), 2 asserting the smoke derivation of that same granted state BLOCKS them again under the same pinned trust root (a future regression that re-couples smoke to its source's live ledger flips these red), and 1 asserting the env channel (the real `APPROVED-013` header) still grants inside the derivation — the neutralization removes the ledger, not authorization. Harness-only by construction: `guard.sh`/`frozen-paths.lib.sh`/`frozen-effect-check.sh` untouched (the `APPROVED-065` HARD constraint — no production-reachable ledger-disable knob). Plus **39 under `decisions/APPROVED-066` [GUARD-4]** extending the two gap classes the PR #116 review of [GUARD-1] proved live to the surfaces GUARD-1 did not own: (1) the long-option **abbreviation ladder** on the PRE-EXISTING git arms — `git reset --har/--ha/--h` (== `--hard`), `git clean --for/--fo/--f` (== `--force`) and `git switch --disc/--dis/--di` (== `--discard-changes`) each measured exit 0 pre-fix in the tokenizer (a directly-invoked `git switch`) while the full spelling blocked THERE, now each blocked down the whole ladder — **corrected 04/08/2026 [GUARD-11] C1r5:** the implicit claim that the full `--discard-changes` blocked *everywhere* was FALSE for a LAUNCHER-wrapped payload (`su -c`/`eval`/`flock -c`), which reaches rule (e)'s raw-text switch arm, not the tokenizer; rule (e)'s ladder stopped at `--discard` and then required a non-dash boundary, so the full `--discard-changes` returned exit 0 there (a repo-wide clean-tree revert with no effect-wall backstop) until C1r5 mirrored the tokenizer's full ladder into rule (e), proven by the new `[GUARD-11 C1r5]` cases in `scripts/test-hooks.sh` — with the false-positive controls that make the ladders discriminating (`reset --soft/--mixed/--merge/--keep`, `clean --dry-run/-n/-n -d -X`, `switch --detach/-c/--create/-C` all stay ALLOWED — `--detach` is the measured `--d` ambiguity neighbor the switch ladder must start past); `git restore --pathspec-from-file=<f>` (both spellings, ladder down to `--pathspec-fr`; `--pathspec-f` is git-rejected ambiguous) conservative-denied because it reads restored paths OFF argv — the `checkout-index --stdin` class — while explicit-path `restore` forms stay path-gated and allowed; `stash` measured needing NO ladder (git does not abbreviate stash SUBCOMMANDS: `git stash po` → exit 128) and read-tree needing none (`-u` is short-only); plus grant-path discrimination (abbreviations honored by a literal `**` Scope exactly like the full spelling; unprotected repo untouched); and (2) the **quoted-token bypass** (§12A class 1, `memory/inbox/2026-07-31-guard1-review-quote-blanking-residual.md`): `neutralize_and_split` now STRIPS quotes and KEEPS content (single, double, dollar-single, dollar-double, backslash escapes; in-quote whitespace carried as an STX marker so a quoted multi-word argument stays one shell word) — `git reset '--hard'`, `git checkout-index '--a' -f`, `git switch '--disc'`, a quoted frozen path under `sed -i`/`tee`, a double-quoted and a dollar-single-quoted `--hard`, and the backslash spelling `--ha\rd` all blocked (each exit 0 pre-fix), with quoted-prose controls proving content-keeping does not false-match from non-arm positions or split quoted segments. **The PR #123 cross-model review (opus) then measured three same-class channels the first cut left live** — each blocked now with abbreviation-adjacent controls: `git clean -i`/`--interactive` (interactive mode deletes without `-f`; the arm's own comment had falsely claimed "none deletes without -f/--force"), `git checkout --pathspec-from-file` (the byte-identical off-argv channel closed on `restore` in the same diff, left open on `checkout`), and `git switch -f`/`--force` (git's `OPT__FORCE` sets `discard_changes`, a shorter spelling that bypassed the whole `--discard-changes` ladder — short-flag equivalents are in APPROVED-066's frame, which scopes the sibling arm as `clean --force/-f`); plus the STX-marker `-C`-with-space round-trip and the two documented-conservative quoted-value false positives (`clean -e '-f'`, `switch -c '--discard-changes'` over-BLOCK, failing safe). Plus **14 under `decisions/APPROVED-067` [GUARD-5]** closing the interpreter IN-PLACE write channels (the §12A auditor-note row-1 `perl -i` entry, measured live on merged main f579293): the four probe cases — `perl -i`, `perl -i.bak`, `ruby -i`, `awk -i inplace` — each blocked against a frozen fixture path (each exit 0 pre-fix while the `sed -i` control blocked), plus the bundled `perl -pi` cluster, the `gawk` spelling and the `inplace=1` command-line assignment; and 6 controls proving the arm is flag-gated AND path-gated rather than blanket (the in-place spellings on a NON-frozen path stay allowed; `perl`/`awk` WITHOUT the in-place spelling reading a frozen path stay allowed; `python3 -c 'print(1)'` stays allowed — pinning that the computed-path inline-program class is stated residue, not an over-broad arm), plus the bound Scope-covered grant case (the arm takes the same authorization path as every other channel). Plus **18 under `decisions/APPROVED-068` [GUARD-6]** sanitizing the ENV binding channel (`$CLAUDE_TICKET`) to [BIND-2] ledger parity (the `[GUARD-6 env-sanitize]` tag, placed beside the `APPROVED-027` list cases they extend, mirroring the `[BIND-2 validation]`/`[BIND-2 CRLF]` group): 4 recovery cases — a wrapping-double-quoted id, a wrapping-single-quoted id, a CR-polluted id, and a CR-inside-the-quotes id each resolve identically to the clean id (each measured exit 2, silently voided, on the pre-fix lib); 3 validation cases — an env token `.*` dropped BEFORE grep interpolation (**measured pre-fix: it wildcard-matched the fixture's `Authorizes:` line and GRANTED**, the exact hazard the [BIND-2] MINOR closed on the ledger side), an invalid-char token denying like unset, and a dropped token never poisoning a valid id beside it (APPROVED-027 posture); 2 no-regression controls (clean single and multi-id values pass through unchanged); the measured 2026-08-01 incident shape itself (whole-VALUE wrapping quotes → one-sided-quoted tokens, NOT a matched pair) still denying — but now VISIBLY; 3 stderr-diagnostic cases — the one-line drop diagnostic fires naming EACH dropped token of the incident shape, fires for an invalid-char drop, and does NOT fire on a clean value (the false-positive control); and 5 from the PR's own cross-model review round (opus) — a NEWLINE-separated and a CRLF-separated list each resolving past the first line (the review BLOCKER: `read -ra` consumed ONE line, so ids after a newline were discarded before the sanitizer with no diagnostic structurally possible — each proven exit 2/zero-stderr pre-fix, closed with `read -d ''`), a CR-before-the-wrapping-quotes id resolving (pinning the pre-strip trim the review's mutation testing showed live but uncovered), and a CR-ONLY value denying like unset WITH a no-diagnostic control (whitespace-only contract — the first cut announced it, contradicting the contract text in four docs). Plus **8 under `decisions/APPROVED-069`** [GUARD-7] pinning the `Authorizes:` matcher's metachar escape on ALL THREE channels that share it — per channel (env `$CLAUDE_TICKET`, verified ledger token, `--ci` commit-message ticket): an `ENV.968` candidate must NOT match an `Authorizes: ENV-968` line (each proven a false GRANT pre-fix, exit 0 → 2) and the exact `ENV-968` still grants (no regression) — plus the contract's other half from this diff's own cross-model review (opus, MINOR): a genuinely dotted id `ENV.968` still matches an `Authorizes: ENV.968` line (`.` remains a LEGAL id character; without this pin, deleting `.` from the charset or from the token would stay suite-green while a documented-legal id class silently stopped resolving) and the undotted `ENV-968` does not match the dotted line (escape symmetry). Plus **14 under `decisions/APPROVED-075`** [GATE-6] — the `security-md-check.sh` DONE-row proving-cell existence-resolution cases (fictional path, undeclared/undeclared-vs-declared `pnpm` script, prose-only, existing-doc, test-shaped-doc, existing-directory, absolute-path, sticky-prose block-class + the accept-class file spellings and the documented-`pnpm`-residue case), which landed **recorded only in §14** and grew the suite `652 → 666` without updating this cell — the M-4 drift the cell has now corrected four times. Plus **3 under `decisions/APPROVED-072`** [GUARD-9] — the merge-resolution SEEN-path blob-equality backstop (`.claude/hooks/frozen-effect-check.sh`): a merge that RE-ALTERS a frozen path an authorized non-merge commit earlier touched to unauthorized content at HEAD is blocked (pre-fix: skipped as seen, exit 0), plus the authorized-merge control (same re-alteration GRANTED when the merge names a covering ticket) and the blob-match FP-guard (a seen path the merge leaves at its authorized end-state still skips cleanly). Plus **41 under `decisions/APPROVED-071`** [GUARD-8] closing the CRITICAL launcher-prefix/path-qualified-binary bypass of the verb dispatch — the measured bypass table (`env git checkout -- <frozen>`, `/usr/bin/git reset --hard`, `timeout 5`/`command`/`env timeout 5 sudo` stacked launchers, `env FOO=bar` KEY=VAL peel, `nice git checkout-index -f -a`, `sudo git clean -f`, `exec git switch --discard-changes`, `nohup git stash pop`, `setsid git read-tree -u`, `stdbuf git restore --pathspec-from-file`, `env`/`/bin/` `sed -i`, `/usr/bin/perl -i`, `env awk -i inplace`, the new `rsync`/`shred`/`ex`/`ed`/`sponge` verbs, and the indirection/`eval`/python-subprocess-list forms rule (e) catches on quote-blind raw text) each exit 2 where they were 0 pre-fix, with 12 launcher-of-innocent-verb / legit-git-op controls staying ALLOWED (env echo, wrapped `git status`, `checkout -b`, `switch main`, `switch -c`, `stash push`, `clean -n`, `reset --soft`, `switch --force-create`, named-path `checkout-index`, `nice sed -i <nonfrozen>`) and 5 authorization cases (a `**` grant unlocks `reset --hard` and a wrapped `clean -f`; a path-scoped grant unlocks a wrapped `checkout --` of a covered path but not an uncovered one, and is not a `**` master key). **Cross-model review round (opus, REQUEST-CHANGES → fixed in-PR):** the review measured BLOCKER-1 — the no-backstop PATH-ATTRIBUTABLE reverts (`checkout -- <path>`, `restore <path>`, named `checkout-index`) were closed only by the normalizer, which it defeated with wrappers the peel did not model (`env -S '<cmd>'` string re-split, `ionice -c 2`/`chrt -b 0` separate/positional VALUE operands skipped as bare flags, `find … -exec … {} +`), each a trace-erasing exit 0; and MAJOR-2 — the `ionice`/`chrt`/`stdbuf` peel arms consumed only `-*` and left the value operand as cmd0. Closed two ways (17 red-first `[GUARD-8 review]` cases): a **wrapper-immune raw-text PATH SCAN** — a path-attributable revert verb co-occurring with a frozen path token blocks unless that path is authorized (per-path, so a scoped grant still allows a wrapped revert of its own path, proven both directions), immune to ALL wrappers by construction rather than by enumerating launchers (the treadmill the review named); and the peel now models `env -S` (re-split), `ionice`/`chrt`/`stdbuf` value operands and `find -exec` so the effect-wall-backed verb writes through those launchers are caught at PreToolUse too, with FP controls (`git checkout main`/`-- <nonfrozen>` allowed, `git log <frozen>` read allowed via the verb gate, `ionice ls`/`find -name`/`env -S 'ls'` allowed). **Cross-model review round 2 (opus, REQUEST-CHANGES → fixed in-PR):** the round-1 fixes held, but the review measured a NEW MAJOR — the per-path raw scan is a text scan for a LITERAL frozen path token, so any frame that supplies the path OUT-OF-BAND defeats it: `find .claude/hooks -type f -exec git checkout -- {} +`, `find . -name guard.sh -exec git restore {} +` (`+` and `\;`), and `xargs git checkout -- < list` each reverted a frozen guardrail at exit 0 (the dir/`-name` argument names no frozen token; `{}`/stdin hides the file), while the block message falsely claimed `find -exec` was covered. Closed (12 red-first `[GUARD-8 review2]` cases) by a find/xargs FRAME arm in rule (e): a `find … -exec/-execdir/-ok/-okdir` or `xargs` launching a path-attributable revert verb (`checkout`/`restore`/`checkout-index`) supplies paths unattributably, so it takes the SAME conservative-deny posture as the whole-repo verbs — blocked unless a `**` grant — closing the out-of-band CLASS rather than the frame list; FP controls (`find -exec ls`, `find | xargs grep checkout` with no `git` token, `xargs git status` with no revert verb stay allowed) and the `**`-lift-vs-path-scoped-deny symmetry proven both directions. The overclaiming message + comments were corrected to state exactly what the per-path scan does and does NOT cover, and the residue it does not close is disclosed honestly. **Cross-model review round 3 (opus, REQUEST-CHANGES → fixed in-PR):** the round-2 frame arm keyed on literal frame NAMES (`xargs`/`-exec`), so it was a frame LIST, not the class — the review measured GNU `parallel git checkout -- {}`, `find … | while read f; do git checkout -- $f`, `for f in $(find …); … $f`, and `gnid=$(…); git checkout -- $gnid` each reverting a frozen guardrail at exit 0 (the first two carry a real `git` token, so they were NOT the disclosed command-substitution/renamed-binary residue — the claim "closes the out-of-band class" was falsified). Closed by SHAPE not name (14 red-first `[GUARD-8 review3]` cases): (i) a stdin/`{}` frame — `xargs`/GNU `parallel`/`find -exec/-execdir/-ok/-okdir` running a revert verb; (ii) a `$`-EXPANSION pathspec — the revert verb's pathspec position is a shell variable or command substitution (`checkout -- $f`, `restore $var`, `checkout -- $(…)`, backtick), keyed on the pathspec POSITION (`--`/`restore`/`checkout-index` immediately followed by `$`/backtick) rather than a bare `$(`/`|`/loop keyword — so `x=$(date); git checkout main` and `git checkout -- <nonfrozen> | tee log` do NOT false-fire (proven). Both gated on the `**`-vs-path-scoped authorization the whole-repo verbs use (path-scoped does not vouch for an out-of-band pathspec; `**` lifts). The residue is now narrower and disclosed precisely, not over-claimed: a git binary invoked under a renamed on-disk path (names no `git` token — pinned at its stated exit 0), a revert mixing a literal and an expansion pathspec where the anchor lands on the literal, and runtime verb CONSTRUCTION — the same token-scanner floor rule (e)'s whole-repo arms carry. Accepted conservative FPs, documented: an expansion-pathspec revert of a NON-frozen file, a `git checkout --` of a path literally containing an `xargs`/`parallel` component, and a compound command pairing an unrelated `-- $var` with a checkout elsewhere each over-block (reword or `**`). **Cross-model review round 4 (opus, REQUEST-CHANGES → fixed in-PR):** round 3's `$`-expansion arm was a raw-text ADJACENCY regex (`--` immediately followed by `$`), which the review defeated by moving the expansion off the anchor — a `./` prefix (`checkout -- ./$f`), a dropped `--` (`checkout $f`), or an INSERTED `--` (`restore -- $f`) — plus `while read f; do git checkout -- $f; done` whose loop body segments to `do git checkout …` (cmd0 was `do`, so no arm ran). Fixed by moving out-of-band detection into the TOKENIZER (14 red-first `[GUARD-8 review4]` cases): `grd8_operand_unattributable` flags any parsed PATHSPEC operand carrying a shell expansion (`$VAR`/`$(…)`/backtick) or the find `{}` placeholder in the `checkout`/`restore`/`checkout-index` arms of `bash_segment_touches_frozen_path`, and the normalizer now peels the shell body keywords `do`/`then`/`else` so a loop/conditional body reaches the verb dispatch. Position-aware, so a prefix, a moved `--`, or a launcher wrapper cannot dodge it (the round-3 raw-text arm (ii) is REMOVED; arm (i) — the `xargs`/`parallel` stdin frame whose operand never reaches the tokenizer — is kept as the stdin backstop). Option VALUES carrying `$` (`restore --source=$REF`, skipped as `-*`) and real branch switches do NOT false-fire. `restore`/`checkout-index` (unambiguous — a pathspec is never a branch) are fully closed **for out-of-band operands**; the residues are honestly disclosed, not silently open: `git checkout <expansion>` WITHOUT `--` is left ALLOWED because closing it would over-block `git checkout $BRANCH` (branch-vs-pathspec ambiguity) — pinned by a residue test at exit 0 — alongside the renamed-binary and runtime-verb-construction floor, and — on a SEPARATE axis surfaced by the review's final round — a BROAD/ANCESTOR literal pathspec (`git checkout -- .`, `git checkout -- .claude/hooks`) reverts frozen CHILDREN unattributed because `path_is_frozen` has no ancestor case; that is pre-existing, a different class than GUARD-8's launcher charter, its fix touches the scope_out `frozen-paths.lib.sh`, and it is TRACKED as `decisions/PENDING-077` (§12A residue (a), the false "effect wall backstop" wording corrected there and in guard.sh). **Cross-model review round 5 (opus, REQUEST-CHANGES → fixed in-PR):** `grd8_operand_unattributable` was a metacharacter DENYLIST (`$`/backtick/`{}`) and missed pathname GLOBBING — a wildcard in the DIRECTORY component (`git checkout -- .claude/*/guard.sh`, `.claude/hook?/guard.sh`, `.claude/[h]ooks/guard.sh`, and the `restore`/`checkout-index` forms the round-4 fix CLAIMED to close) obscures the literal `.claude/hooks` prefix, so `path_is_frozen` (which sees the un-expanded literal glob) also misses it while bash expands the pattern to the frozen file at runtime — each a no-backstop revert at exit 0 (measured). Fixed (10 red-first `[GUARD-8 review5]` cases) by FLIPPING the predicate from a metachar denylist to a literal-path ALLOWLIST: attributable iff the whole operand is in a safe path charset, else conservative-deny — which closes `$`-expansion, `*`/`?`/`[…]` globs, brace lists, and tilde in ONE predicate rather than enumerating dangerous chars (rubric #6 — require provable literalness). A LITERAL path stays attributable, so per-path authorization and the literal-frozen block both survive (proven). Accepted fail-safe cost, documented not hidden: a globbed/expansion-built revert of NON-frozen files (`git checkout -- src/*.ts`) is `**`-gated over-blocked — the correct posture for a no-backstop control that cannot know statically what a pattern expands to, recoverable with a `**` grant. **Cross-model review round 5b (opus, REQUEST-CHANGES → fixed in-PR):** the round-5 "safe" charset admitted `:`, git's PATHSPEC-MAGIC sigil — `:/path` is repo-top-relative, `:!`/`:^`/`:(…)` are exclude/attr forms — so an all-otherwise-safe operand `:/.claude/hooks/guard.sh` (and its `restore`/`checkout-index`/whole-frozen-directory variants) was judged attributable while `path_is_frozen`/`canon_path` (which do not model git's pathspec grammar) canonicalized the literal `:/…` to a non-match and git reverted the real frozen file at runtime (exit 0, measured). Fixed (7 red-first `[GUARD-8 review5b]` cases) by dropping `:` from the charset (now `[A-Za-z0-9._/@+-]`), denying the whole magic-pathspec class in one char; no frozen path contains `:`, so it costs nothing legitimate. `@` is KEPT (git rev-magic `@{…}` is already denied by the brace; a plain `@` after `--` is a literal filename — pinned by a `docs/a@v2.md`-stays-allowed control). Plus **1** in the GUARD-4 FP section: a tokenizer-quote-neutralization control re-pinned with a non-revert signature when two prose cases (`echo`/`commit -m` literally spelling `reset --hard`) were reclassified as GUARD-8 accepted conservative FPs (rule (e) is quote-blind BY DESIGN — that is its wrapper immunity), and the `-C <other repo> reset --hard` case likewise flipped to a documented accepted FP (the no-backstop revert takes the whole-command posture; the `-C` scoping guarantee holds unchanged for the effect-wall-backed `mv` channel, proven by the sibling case). 291 + 20 + 12 + 6 + 15 + 10 + 20 + 5 + 9 + 32 + 20 + 21 + 15 + 51 + 19 + 5 + 61 + 14 + 18 + 8 + 14 + 3 + 41 + 17 + 12 + 14 + 14 + 10 + 7 + 4 + 1 + 31 = 820. **Total and breakdown re-measured 30/07/2026 by running the suite, not carried forward** (audit M-4: this cell read `343` over a breakdown that omitted the hotfix component entirely, while the changelog below already recorded the suite at `353` — one document disagreeing with itself, in a cell whose whole purpose is to be a claim to an auditor). **The same drift recurred and is corrected here** [SEC-13]: this cell read `378` with a breakdown summing to `378` while the live suite stood at `387`, because [SEC-11] added its 9 cases and recorded them only in the §14 changelog. Both the missing component and the new one are itemized above, so the sum is checkable against `pnpm test:hooks` rather than trusted. **And once more, corrected here** [ORG-5]: this cell's headline read `378` while the breakdown immediately below it already summed to `419` — the document disagreeing with itself *within a single cell*, which is the M-4 shape at its shortest possible range. Headline and sum are both `652` as of the [GUARD-7] matcher metachar escape (`decisions/APPROVED-069`, 01/08/2026, including its review-round cases), re-measured by running the suite (`652 cases run, 0 failed`). **And the M-4 shape recurred a third time, IN THE GUARD-7 COMMIT ITSELF, caught by its cross-model review (opus, MAJOR) before merge:** the first cut updated this cell's breakdown and its own "headline and sum" sentence to `650` while the headline token at the cell's head still read `644` — the within-cell disagreement at exactly the [ORG-5] shape, in the commit whose changelog row cites it. No gate asserts this count (measured: nothing in the gates chain greps it), so review remains the only control; corrected here with the review-round cases folded in. **Reconciled again to `669` as of [GUARD-9] (`decisions/APPROVED-072`, 02/08/2026):** [GATE-6]'s 14 `security-md-check` cases had landed recorded only in §14 (the fourth occurrence of the M-4 drift, itemized above), and [GUARD-9] adds 3 — both components are now in the breakdown and the sum is checkable against `pnpm test:hooks`. **Reconciled to `768` as of [GUARD-8] (`decisions/APPROVED-071`, 02/08/2026):** +41 initial GUARD-8 cases, +17 first-review-round cases, +12 second-review-round cases (the find/xargs out-of-band frame arm), +14 third-review-round cases (stdin/`{}` frames incl. GNU `parallel`, and the round-3 `$`-expansion raw-text arm), +14 fourth-review-round cases (moving out-of-band detection into the tokenizer by operand SHAPE — closing the prefix/dropped-`--`/inserted-`--`/loop-body spellings the round-3 regex missed — plus the disclosed no-`--` residue pin, the option-value/branch-switch FP controls, and the operand-shape authorization cases), +10 fifth-review-round cases (flipping the operand predicate from a metachar denylist to a literal-path allowlist — closing the pathname-glob directory-component reverts across checkout/restore/checkout-index, plus the literal-still-attributable proof and the `**`-gated non-frozen-glob fail-safe), +7 fifth-b-review-round cases (dropping `:` from the charset to close git's `:/`/`:!`/`:^` magic-pathspec reverts, plus the `@`-kept literal control), +4 sixth-review-round residue-pin cases (pinning the pre-existing broad/ancestor literal-pathspec revert gap — `git checkout -- .`, `git restore .`, `git checkout -- .claude/hooks` — at its measured exit 0 with a non-frozen-subtree control, tracked as `decisions/PENDING-077`, a class distinct from GUARD-8's charter whose fix touches the scope_out `frozen-paths.lib.sh`), and +1 net in the GUARD-4 FP section, all itemized above; the sum is re-measured by running the suite (`789 cases run, 0 failed`), not carried forward. **Reconciled to `820` as of [GUARD-10] (`decisions/APPROVED-077`, 02/08/2026, including its cross-model review round):** +31 — the 4 sixth-GUARD-8-review-round residue-pin cases stay counted in their +4 component but the 3 broad/ancestor pins among them FLIP from exit 0 to exit 2 (the residue is closed, so the pins become the regression proof), and the 31 new cases add: the remaining measured APPROVED-077 tokenizer spellings (`./`, grandparent dir, ref-sourced `HEAD --`, restore-of-dir, checkout-index-dir, `scripts` prefix-ancestor, `..` above the root), the site-2 fixture discrimination pairs in GATE1_REPO/ROUTING_REPO (the ancestor walk reads the same candidate list the repo-wide verbs use, so only a sole-protected-path repo proves it discriminates on that list), the binding trio (`**` lifts, path-scoped does not, literal in-scope revert under a path-grant survives), and — from the opus cross-model review, which measured the FIRST cut BLOCK because the ancestor check was placed ONLY in the tokenizer and so was defeated by the no-`--` ref form (`git checkout HEAD .`) and by launchers the cmd0 peel does not enumerate (`setarch`/`taskset`/`systemd-run`, effect-verified reverting the frozen set at exit 0) — the review-round cases proving the fix that moved the ancestor wall into the wrapper-immune raw-text rule (e): 6 no-`--` ref forms, 4 launcher-wrapped forms, the discrimination controls (single-operand `git checkout main`, `git status .`, `find . | xargs grep -l checkout` with no `git` word), and the documented accepted-FP pins (`git checkout main; git add .`, `-C <unrelated repo>`) — plus the GUARD-8 review-4 loop residue that the raw arm now closes because its `find .claude/hooks` names the ancestor dir. The sum is re-measured by running the suite (`820 cases run, 0 failed`), not carried forward. **Reconciled to `838` as of [GATE-5]/[GATE-5A] (`decisions/APPROVED-074`, `decisions/APPROVED-076`, 02/08/2026):** +18 freezing the typecheck gate's config pair (`tsconfig.json` + `tsconfig.base.json`) at both enumeration sites and its prover `scripts/probe-typecheck-gate.sh` — 4 single-path BLOCK cases (Edit/Write/Bash over the pair), 3 bound-in-`Scope` GATE-5 grant + scope-stays-narrow cases, 9 in the new `TSCONFIG_REPO` fixture whose ONLY protected paths are the pair so the site-2 `repo_has_protected_paths` walk discriminates on its own (the four repo-wide git verbs, the GUARD-10 broad/ancestor `git checkout -- .` and no-`--` `git checkout HEAD .` reverts, the lone single-path Edit, and the `.editorconfig`/`src/app.ts` false-positive controls), and 2 freezing the prover against Edit and Bash-stub. The sum is re-measured by running the suite (`859 cases run, 0 failed` as of APPROVED-083 — 838 at this row's last full recount, +9 `DOCINT_REPO` cases [OPS-16], +12 `DASHW_REPO`/binding cases freezing `scripts/test-dashboard-writer-header.mjs` [APPROVED-083]; `MIN_CASES` floor ratcheted 847 → 859), not carried forward. **Reconciled to `897` as of [GUARD-11] (`decisions/APPROVED-084`, 04/08/2026):** +38 closing the four protection-neutering gaps the 2026-08-03 audit found — **C1** (CRITICAL) the launcher-peel cross-product: each UNENUMERATED launcher (`flock`/`taskset`/`setarch`/`systemd-run`, and `unshare`/`nsenter` spellings) in front of a git REVERT verb with a `:/`-magic or glob pathspec now conservative-denies (measured exit 0 pre-fix — the tokenizer's git arm never ran because cmd0 was the launcher, and the raw literal path scan missed the non-literal pathspec; both conditions were necessary), plus the enumerated-launcher/bare-form controls that were already blocked and the non-revert/non-git FP controls that stay allowed, plus the `**`-lift-vs-path-scoped-deny symmetry (an unattributable operand needs a `**` grant); **M1** the bare-directory freeze for `.claude/hooks` and `.github/workflows` (rm/mv/Edit of the CONTAINER, not only its children — each exit 0 pre-fix, the asymmetry vs `.claude/skills`); **F1** the redirect arm now matching fd-qualified (`N>`/`N>>`), stdout+stderr (`&>`/`&>>`) and clobber-override (`>|`) targets, path attached or detached (the `>|` needing a `neutralize_and_split` fix so the `|` pipe metachar no longer severs the clobber operator), promoted from the effect-only rows (APPROVED-021 table rows 6, 22) to PreToolUse blocks, with non-frozen-target and fd-dup (`2>&1`) FP controls and a path-scoped grant lifting a covered redirect; and **G1** freezing the new self-pin prover `scripts/probe-dashboard-writer.sh` at both enumeration sites (site-1 Edit/redirect block + bound-in-Scope grant). The sum is re-measured by running the suite (`897 cases run, 0 failed`), not carried forward; `MIN_CASES` ratcheted 859 → 897. The 100 effect-check cases differ in kind from the rest: each **executes** its payload for real in a throwaway fixture repo and then asks the check what changed on disk, rather than feeding a hook a string and asserting how it parses it | `pnpm test:hooks` |
| Frozen-path effect check | `.claude/hooks/frozen-effect-check.sh --ci` | Every frozen path changed in the range is authorized **per commit** (`decisions/APPROVED-025` Option A, [DC-46]): for each non-merge commit between the base ref and HEAD, the frozen paths *that commit* changed must be covered by the `Scope:` of a ticket named in *that commit's own* message — a covering ticket named by a neighbouring commit vouches for nothing, which closes the one-scoped-commit-launders-the-branch gap (§12A effect-check limitation (6)) and removes the first-push special case. A **whole-range backstop** covers what per-commit attribution structurally cannot see: a merge commit's own conflict resolution can write frozen content belonging to no parent (an "evil merge") or discard a base fix ("take ours"), so any frozen path changed across `MERGE_BASE..HEAD` that no non-merge commit authored must be covered by `$CLAUDE_TICKET` or a ticket the merge commit itself names — a post-review [DC-46] hotfix after the first cut skipped merge commits and let an evil merge pass. Runs as its own stable-named `frozen-effect` CI job, and **is now a required status check** on `main` (CEO ruleset change, 28/07/2026) — established by a controlled A/B on one throwaway pull request (#5), not by reading a settings page. Same PR, same base, two head SHAs: with a failing `frozen-effect` check run the PR reported `mergeable_state: blocked`; with `frozen-effect` green and only `gates` still pending it reported `unstable`. `unstable` is returned only when nothing *blocking* is outstanding, which excludes a required-review explanation and confirms `gates` is not required, so the block on the first SHA is attributable to the required check. **Updated 28/07/2026:** `gates` is **now required too**, measured the same way on PR #7 — a failing `gates` with `frozen-effect` green reported `blocked`, where PR #5 had reported `unstable` for the same shape before the ruleset change. Both CI jobs now stop a merge | `pnpm frozen-effect:ci` |
| This matrix | `.claude/hooks/security-md-check.sh --ci` | No row claims `DONE` without a named proving test — in `--ci` mode, so edits made outside a Claude session are still checked | `pnpm security-md:ci` |
| SCA | `pnpm audit --audit-level=high` over the pinned `pnpm-lock.yaml` ([SEC-5], `decisions/APPROVED-030`) | No dependency in the lockfile carries a known high/critical advisory. Tool choice measured, not assumed: the audit endpoint answered reliably in ~0.7s from both this container and a bogus-registry control, so `pnpm audit` was chosen over `osv-scanner` (no second binary to install and pin). **Fail-closed offline:** an unreachable advisory source exits non-zero with a connection error — measured, and re-proven on every chain run by the probe; a network error never prints green. One triaged ignore, recorded not hidden: `GHSA-mh99-v99m-4gvg` is in `pnpm.auditConfig.ignoreGhsas`. **Revisit condition made falsifiable 29/07/2026 [SEC-9]** (`decisions/APPROVED-039` Option B) — the previous wording, "revisit when the eslint/minimatch chain moves", could not be checked by looking. The measured chain: the advisory's affected range is **`≤ 5.0.7`, unbounded below**, first patched **5.0.8**, so the installed `brace-expansion@1.1.16` is genuinely in scope — this was checked, it is **not** a false positive of pnpm's range matching; the vulnerable path is `eslint@9.39.5 → @eslint/config-array@0.21.2 → minimatch@3.1.5 → brace-expansion@1.x`; **`9.39.5` is already the terminal 9.x release** and still pins `@eslint/config-array ^0.21.2`, so **no 9.x bump can clear it**; upstream fixed it only on the next major (`@eslint/config-array@0.23.5 → minimatch@^10.2.4 → brace-expansion@^5.0.8`), reachable only via **eslint 10** (latest `10.8.0`); and a direct override to 5.0.8 was tried at [SEC-5] time and **measured breaking eslint at require time** (`TypeError: expand is not a function` — 5.x exports `{expand}` named where `minimatch@3` requires a callable module), so the override route is closed while `minimatch@3` is in the tree. **What justifies suppressing a high:** a dev-time-only DoS in the lint toolchain — not shipped, not reachable from product code. (**One leg of this argument was removed 04/08/2026:** the clause used to end *"and `packages/` currently declares no production dependencies"*, which [DC-2] `ab8a186` made false — `packages/db` now declares `drizzle-orm@0.45.2` and `postgres@3.4.9`, the repo's first runtime dependencies, measured from the six `packages/*/package.json` files. The suppression still stands on its remaining terms: the vulnerable path is `eslint → @eslint/config-array → minimatch → brace-expansion`, reachable from no product runtime import. The "nothing ships at all" backstop is gone and does not come back.) **Trigger:** the ignore clears on the eslint 10 upgrade and nowhere else (`APPROVED-039` Option A / `SEC-9`'s deferred sibling), owed when `typescript-eslint` ships stable eslint-10 support. **Scoping is deliberate:** `ignoreGhsas` carries exactly one id, so a *second* advisory on this same path would still redden the gate. **Documented residue, added 04/08/2026 [DC-2] — not an ignore, a threshold pass:** `pnpm audit --json` now reports exactly one advisory below the gate's bar — `GHSA-67mh-4wv8-2f99` (**moderate**; esbuild dev-server reads any origin's request), on `packages__db > drizzle-kit > @esbuild-kit/esm-loader > @esbuild-kit/core-utils > esbuild@0.18.20`. Measured, not inferred: `pnpm audit --audit-level=high` exits **0** and the JSON metadata reads `{"moderate":1,"high":0,"critical":0}`, so nothing was ignored or configured away — the gate's `--audit-level=high` threshold simply does not judge it. Why it is accepted rather than fixed: `drizzle-kit` is a **devDependency** of `packages/db` (a generator, `drizzle.config.ts`, output redirected to gitignored `drizzle-kit-out/`), the advisory's code path is esbuild's **dev server**, which nothing in this repo starts, and the fix is not reachable by an override — the vulnerable `esbuild@0.18.20` is pinned inside the deprecated `@esbuild-kit/*` chain, while the tree's other esbuilds (`0.25.12`, `0.28.1`, vite/vitest) are already patched. **Revisit condition (falsifiable by looking, per `APPROVED-039` Option B's rule):** it clears when `drizzle-kit` drops `@esbuild-kit/*` (upstream merged that loader into `tsx`, which `drizzle-kit@0.31.10` *already* also depends on — it carries both) — check with `pnpm --filter @auto-mb/db why esbuild`, which today prints three versions, `0.18.20` under `@esbuild-kit/core-utils` and the patched `0.25.12`/`0.28.1` under `drizzle-kit`/`tsx` directly (root-scoped `pnpm why esbuild` sees only the root's `0.28.1` and will not show this — the wrong command answers "clear" every time). If the `@esbuild-kit` arm is gone and `pnpm audit` still reports the advisory, this residue is wrong and the row is owed a re-measurement. It also stops being a *residue* the moment the advisory is re-rated high/critical, at which point the gate reddens on its own. Probe: `scripts/probe-sca-gate.sh` — an embedded lockfile pinning `lodash@4.17.15` (3 high advisories) drives the audit red, the real lockfile passes, the offline path exits non-zero. Cost: gate ~0.7s, probe ~1.4s (offline-case retries pinned to zero after the default backoff measured ~70s) | `pnpm sca`, `pnpm sca:probe` |
| SAST | semgrep 1.172.0 (engine pinned in `.github/workflows/ci.yml`) over `packages/**` with the repo-committed, pinned ruleset `.semgrep.yml` ([SEC-6], `decisions/APPROVED-030`) | No product code carries the enabled finding classes: **string-built** SQL query arguments (CWE-89 — the query-parameterization law, AGENTS.md non-negotiable 5) — inline concatenation/interpolation and, **since [SEC-7], the assign-then-pass, `.concat()` and array-`join` builder shapes** — `eval`/`new Function` (CWE-95; since [SEC-7] enabled `no-new-func`, this is defense-in-depth for *both*, behind SEC-4's lint bans), and `child_process` exec with concatenated/interpolated arguments (CWE-78), inline and assigned forms alike. **Widened 29/07/2026 [SEC-7]** (`decisions/APPROVED-037` Option A, closing F7; round 2 after the cross-model review): two taint-mode rules (`automb-sql-assigned-string-built-query`, `automb-command-injection-assigned-concat`) track a built string to the sink through intermediate variables and builders — `const q = "…" + x; db.query(q)`, the commonest real shape, now fires; every source requires a **non-constant operand**, and the concat sources additionally exclude `.concat()` results and nested `+` operands — when such an operand really carries taint, it originated at a `.concat()` argument or an inner literal-adjacent concat, each already a source, and propagation carries it to the sink (probe-guarded). **A `.join()` exclusion was tried in review round 3 and REVERTED in round 4** after the reviewer measured it silently dropping the caller-supplied-array class — `"… IN (" + ids.join(",") + ")"` where `ids` is a parameter has no in-function array literal for taint to originate at, so the exclusion cost 3 true positives the merged round-2 ruleset catches. The revert is a **deliberate FP-vs-FN choice, recorded not buried**: a literal-prefixed join over a provably-constant array (the const `clauses.push` assembly, the canonical parameterized IN-list builder, an all-literal array-join command) is syntactically identical to the hostile shape and semgrep cannot const-propagate arrays, so those shapes FIRE as **accepted false positives** — pinned in the probe as documentation cases (going quiet reddens the probe until the author proves the caller-supplied class still fires), rewrite: inline the constant fragments or use `= ANY($1)`. Same conservative doctrine as the kernel-segment lint rule ("renaming is cheaper than a hole"). What stays lawful and quiet, probe-asserted: parameterized + `execFile`, constant assign-then-pass, an all-literal array joined and passed **directly** (no literal-adjacent concat), const-propagated conditional reassembly (`q = q + " literal"`), module-const template, const base + const suffix, constant command. **Stated bounds:** taint enters array builders at array **literals** only (mutation/derivation/parameter arrays are not tracked as arrays — their `.join()` is caught only when spliced next to a literal), and a builder result passed directly to a sink with no literal-adjacent concat and no in-function array literal (`db.query(ids.join(","))`) is uncovered — true of the merged ruleset too, stated rather than implied. **Correction from the same review, recorded not glossed: an inline `// nosemgrep` comment in the scanned file CAN suppress a semgrep finding** — the previous claim that this gate "cannot be switched off from the scanned file" was false; both layers (lint + SAST) are suppressible by a reviewer-visible inline comment, and running the gate with `--disable-nosemgrep` is owed to a `package.json`-scoped follow-up (the `sast` invocation is frozen outside SEC-7's scope; proposed via `memory/inbox/`). Honest bounds: taint tracking is **intraprocedural** — a query string built in one function and passed to a sink in another stays uncovered; and semgrep 1.172.0 does not scan `.mts`/`.cts` files (measured), so for those extensions the [SEC-7] lint layer is the only automated cover. Rules are **vendored into `.semgrep.yml`**, adapted from semgrep-rules' security-audit families and mapped to OWASP/CWE in metadata — no registry fetch, no floating `latest` at scan time (`--metrics=off --disable-version-check`; the APPROVED-006 supply-chain posture). Runs **in the `pnpm gates` chain** (the implementer's measured call: scan ~2s, probe ~7s, well under the 60s budget — no new CI job, so no new required-check owner action). Fail-closed: missing binary → command-not-found non-zero; missing ruleset → semgrep config error non-zero; both probe-asserted. Probe: `scripts/probe-semgrep-gate.sh` — fixtures carrying each class drive the scan red with all **five** rule ids firing, and each assigned-form sub-class (assign+concat, assign+template, `.concat()`, array-join, **no-separator `.join()`** — a round-2 addition after the review measured `$ARR.join($SEP)` missing it) is pinned to its exact finding (check_id + fixture line — a rule id firing once could mask a blind sub-class, and a substring match would be the incidental-assertion class); the false-positive side is asserted in BOTH directions: the lawful-and-quiet shapes (parameterized + `execFile`, constant assign-then-pass, all-literal direct join, const-propagated reassembly, module-const template, const base + const suffix, constant command) each a control, and the **accepted-FP shapes pinned firing** as documentation (const `clauses.push` assembly, parameterized IN-list builder, all-literal array-join command — each with the gap message naming the proof obligation before accepting their silence); the caller-supplied-array class is pinned firing at three shapes (assigned IN-list, assigned exec join, inline IN-list), and a **tainted array joined then concatenated** guards the retained exclusions; plus the real `packages/` tree. **Sink-coverage bound, measured 04/08/2026 (audit of [DC-2] `ab8a186`; not closed by this row):** both SQL rules' sinks are `$DB.query` / `$DB.execute` / `$DB.raw` only, and [DC-2] introduced the repo's first raw-SQL execution sink under a *different* name — postgres.js `sql.unsafe()`, at its `await sql.unsafe(...)` call sites in `packages/db/src/migration-runner.ts` (**7**, unchanged at `ab8a186`, [DC-3] `5d45235` and the [DC-4] fan-in `65c2a24` — the "5" this cell claimed when it was written on 04/08/2026 was the auditor's own miscount off a `grep unsafe(` that counted doc-comment mentions; re-counted with `grep -c 'await sql.unsafe('` at both commits). Probe run in an isolated scratch file under `packages/db` (deleted after the run), the SAME template-literal payload — an interpolated `table` identifier — on two adjacent lines: passed to `db.execute(...)` it is **2 findings** (`automb-sql-string-built-query` and `automb-sql-assigned-string-built-query`, both reported at that line); passed to `sql.unsafe(...)` it is **0 findings**. So the CWE-89 claim above is a claim about the *named sinks*, not about all raw-SQL execution in product code, and a `.unsafe` sink is exactly what a future feature-code author would reach for. Owed: add `$DB.unsafe($Q, ...)` to both rules' `pattern-sinks` with a probe case per rule (`.semgrep.yml` is a frozen path — needs its own ticket + scoped decision; named here, not filed) | `pnpm sast`, `pnpm sast:probe` |
| Ticket DAG | `scripts/tickets-check.sh` + its negative probe `scripts/probe-tickets-check.sh` | Every file in `tickets/` is a valid ticket per AGENTS.md rule 6 / DESIGN.md §5.2: required front-matter fields present and non-empty, `verify:` a **command** rather than human judgment, id equal to filename and unique, no repeated front-matter key, `risk` in the allowed set, `gate:CEO` tickets resolving `gate_decision` by decision **number** to a live (non-`SUPERSEDED`, non-template) decision, no dangling `depends_on`, no dependency cycle. The probe is in the chain for the same reason `test:hooks` is: a gate whose prover never runs rots silently — its **59** planted-fault and false-positive-guard cases (56 at creation, +3 `gate_decision` cases in `872a1c8`; count re-measured by running the probe, not copied forward) assert the checker fires on every fault class and stays silent on legitimate shapes (measured ~4.5s, far under the 60s budget that would have demoted it to hand-run). Wired 28/07/2026 and proven **discriminating, not just green**: a planted `verify: looks correct` ticket drove `pnpm gates` red at this step; plant deleted, the chain exits 0. Structure only — no ticket's `verify:` command is executed by this gate. **Both scripts are frozen paths since [PENDING-029]** (`decisions/APPROVED-029`): an agent edit to either needs a bound ticket whose decision `Scope:` covers it, proving cases in `scripts/test-hooks.sh` | `pnpm tickets:ci`, `pnpm tickets:probe` |
| Dashboard status truth-flow | `node scripts/gen-status-json.mjs --check` ([DASH-3], `decisions/APPROVED-087` Option B — chain step `status:check` inserted after `tickets:probe`) | What the exit code DECIDES: `docs/data/status.json` (the CEO dashboard's Work-graph data) is byte-identical modulo `generated_at` to a regeneration from `tickets/` — so a hand-edit of the data file, or any DC-ticket frontmatter change without a committed regeneration, reddens the chain. That is the drift class that showed 0% with 15 DC tickets merged (root cause: the frozen generator hardcoded `'pending'`; the interim hand-edit, PR #195, fixed the display while leaving this very comparison red). Red-driven in-session before landing: hand-edited `status.json` vs pending-only regeneration → exit 1; frontmatter honor + 15-ticket backfill → exit 0, idempotent. What the exit code does NOT decide, recorded rather than implied (DASH-3 cross-model review round 1): **(a)** the generator's status-vocabulary semantics (`done`/`in-progress` pass through; key-absent → `pending`, the only silent path; key-present-but-unusable — unknown token, empty, whitespace — → stderr warning + `pending`, the APPROVED-051 fail-soft) were hand-measured at landing 2026-08-05 and are exercised by NO automated check: the real tree carries no bad-status ticket, and a fixture-driven prover needs a tickets-dir override the frozen script lacks (a frozen-file change owed its own decision, riding the prover batch). Mitigation shipped: the summary line both modes print carries the degraded-status count, so the fail-soft cannot vanish into a chain log. **(b)** the step has NO self-pin probe — redefining `"status:check": "true"` in package.json leaves all 29 steps green (measured against the live portability-check by the review); the exception and its closing decision are recorded in org/manifest.json `probes_self_pin_their_step`, per that rule's own recording law. Zero new freezes at wiring: the generator has been frozen at both `frozen-paths.lib.sh` sites since `APPROVED-029` (rule 10 pre-satisfied) | `pnpm status:check` |
| Model routing | `scripts/routing-check.py` | ROUTING.md §3's lane/frontmatter table, `.claude/agents/*.md` frontmatter and `docs/data/status.json` `agents[]` all agree, and every lane `lane.sh` maps resolves to a `model_name` the gateway config declares. Wired by `decisions/APPROVED-018`; the negative probe `scripts/probe-routing-check.sh` plants five real drifts and asserts it fails on each. **Both scripts are frozen paths since [PENDING-029]** (`decisions/APPROVED-029`), with proving cases in `scripts/test-hooks.sh` | `pnpm routing-check` |
| Harness portability | `scripts/portability-check.sh` (org/manifest.json ↔ `.claude/**` drift, MIGRATION/ENVIRONMENT coverage — AGENTS.md rule 9's mechanical subset) + negative probe `scripts/probe-portability-check.sh` | Harness surfaces agree with `org/manifest.json`: seats ↔ `.claude/agents` frontmatter byte-exact, skills/hooks covered in manifest + MIGRATION/hooks-contract, settings wiring resolves, manifest env vars documented, live env references ⊆ manifest, manifest `.gates` ↔ the real chain both directions, MIGRATION.md's required sections present, and — since [GATE-2] (`decisions/APPROVED-049`, rule 10) — **every script the `gates` chain invokes is frozen at both of `frozen-paths.lib.sh`'s enumeration sites**, which is what turns "wired into the chain" and "frozen" from two unlinked manual steps into one change (audit H-3 was exactly that gap: `APPROVED-040` did the first for chain steps 18–20 and not the second). Wired 29/07/2026 (`decisions/APPROVED-034` Option A, [MIG-2]), probe in-chain for the same reason `tickets:probe` is (a prover run by hand stops being run). **Proven discriminating in-chain by fault injection**, not just green: a planted `.claude/skills/rogue-skill/` (installed, absent from the manifest) drove `pnpm gates` red at the `portability:ci` step; plant removed, the full chain exits 0. A first plant — a seatless `.claude/agents/rogue.md` — went red at `routing-check` instead, which also validates agent files: that drift class is double-covered and the chain stops at whichever gate runs first, recorded here so a future probe author picks a plant the portability gate alone catches. Cost measured after wiring: `portability:ci` ~0.6s, `portability:probe` ~4.3s, full chain ~48s — under the 60s-per-check budget that would demote a check to hand-run. Both scripts are frozen paths since [PENDING-029]. From here on, rule 9's mechanical half polices itself on every push; the non-mechanical remainder stays review law (docs/MIGRATION.md "What only review can catch") | `pnpm portability:ci`, `pnpm portability:probe` |
| Dashboard + memory provers | `scripts/probe-branch-select.mjs` + `scripts/probe-dashboard-approve.mjs` (governance dashboard), `scripts/memory-check.sh --ci` + `scripts/memory-check-test.sh` (memory wiki Lint) ([GATE-1], `decisions/APPROVED-040` Option A) | The branch picker's anti-spoofing/ordering/degrade properties (30 assertions) and the PR-based approval flow against a **stateful** GitHub fake enforcing real contents-API preconditions (89 assertions) hold on every chain run, not only when someone remembers a hand run; `memory/`'s structural rules fail the chain (exit 2) while staleness findings stay report-only but now print in the CI log where they get read; the checker's own 22-case suite runs in-chain so a checker regression that stays green on the clean tree (the mawk-interval class) still reddens the push. All three steps fault-injection-proven in-chain, every plant in a throwaway clone. Cost measured at wiring: ~2.8s combined as chain steps (~1.9s script time + pnpm wrapper). Substance, provenance and residues live in the three §12A rows this wiring closes limits of — including **two wiring-borne false-positive classes found at review, measured and recorded there** (a correct `..`-escaping relative link out of `memory/` misreported as dangling; runner-UTC `as_of` future-dating) — the §12A memory-Lint row names both with their owed follow-up. **All four scripts are frozen paths since [GATE-2]** (`decisions/APPROVED-049`, closing audit finding H-3) — the sentence every sibling gate-script row in this table carries, and the only row that lacked it until 30/07/2026. The finding it closes, for the record: `APPROVED-029` froze the gate chain's links, the [GATE-1] wiring (`APPROVED-040`) added chain steps 18–20 the next day and never added its own four, so for one day an **unbound** agent — no ticket, no decision, no approval — could rewrite three chain steps *and their provers*, including the checker/prover pair `memory-check.sh` + `memory-check-test.sh` whose joint gutting the chain structurally cannot see (measured then: both stubbed, full 21-step `pnpm gates` exit 0). Re-measured 30/07/2026 with `CLAUDE_TICKET` unset, Edit payloads fed to the live `.claude/hooks/guard.sh`: each of the four now exits **2 (blocked)** where each exited **0 (allowed)** at `e98cfd6`, while `scripts/lane.sh` — invoked by no gate step — still exits **0**, so this is a link and not a blanket freeze of `scripts/`. Frozen at **both** enumeration sites in `.claude/hooks/frozen-paths.lib.sh`: `is_protected_path` (Edit/Write and single-path Bash channels) and `repo_has_protected_paths` (the repo-wide git verbs — `apply`/`am`/`stash pop`/`clean -f`/`reset --hard`/`switch --discard-changes` — which cannot attribute a target from argv). Patching only the first was **measured** to leave the second blind: with the four site-2 entries stripped and site 1 fully intact, `repo_has_protected_paths` reports *no* protected path for a repo whose only protected path is `memory-check.sh`, while `is_protected_path` still calls it frozen. That is why the proving cases use a dedicated `GATE1_REPO` fixture holding exactly one GATE-1 script instead of reusing `KERNEL_REPO`, where the list returns on its first match — `packages/kernel` — long before it reaches `scripts/`, so a repo-wide case there passes identically whether or not site 2 was ever patched. **The class is closed, not only the instance:** `scripts/portability-check.sh` rule 10 fails when any script the `gates` chain invokes is absent from either site, linking the two previously unlinked manual steps (wire a gate / freeze a gate) that produced H-3. Three plants in `scripts/probe-portability-check.sh` assert it fires — unfrozen-everywhere, site-1-only, and site-2-only — each asserting the *message* rather than only the exit code, since a plant that trips a different rule would flip the exit code and read as proof; plus a `lane.sh` control asserting the rule does not over-freeze. Config files a gate merely reads are deliberately out of rule 10's scope: the prettier pair is `APPROVED-044`/`ORG-5`'s work and is still unfrozen (see the Formatter row) | `pnpm dashboard:probe`, `pnpm memory:ci`, `pnpm memory:probe` |
| Dashboard writer header discipline | `scripts/test-dashboard-writer-header.mjs` (APPROVED-083, the [DASH-2] follow-up — chain step inserted after `dashboard:probe`) | The regression guard for the decision-writer's twice-shipped double-header defect (APPROVED-058, CEO hand-edit `74ca437`; APPROVED-080 → PENDING-081/APPROVED-081, OPS-15/OPS-16 halted mid-sprint by an inert grant that read as live): 112 checks over clean/gapped/doubled(stale-valued)/pre-H1/prosey/unlisted-key/duplicate-split/placeholder fixtures asserting the emitted decision carries EXACTLY ONE contiguous header block immediately after the H1, a hand mirror of the guard's `frozen_header_scan_stdin` walk reads back the CEO's typed grants verbatim (parser-agreement), the grant-nothing placeholder family (NONE-PLACEHOLDER incl. wrapped/case-shifted spellings, NONE/TBD/N-A/TODO/dashes) refuses before any network call, non-blank pre-H1 content refuses (`InertApprovalError`), and the approve flow's emission POST-CONDITION (guard-walk read-back must equal the typed grant) fails closed on shapes the known-key healer does not recognize. Mutation-proven discriminating at wiring: 8 writer mutants (no-normalize, keep-last dedup, no-placeholder, no-first-line-check, no-leading-blank-heal, shape-only merge, post-condition disabled, mirror first-wins → last-wins) each drive ≥1 named GAP; cross-model review (opus, 3 rounds on the DASH-2 PR) differential-tested the mirror against the real bash parser (900 docs, 0 divergence) and 500 generated files end-to-end (0 inert/wrong emissions; 83/83 real decision files, 0 false refusals). **Frozen at both `frozen-paths.lib.sh` enumeration sites in the same change that wired it** (rule 10 pairing, APPROVED-049 doctrine); 12 suite cases prove both sites plus both binding halves (a Scope-covered bound approval grants, wrong-Scope blocks) with `DASHW_REPO` as the sole-protected-path site-2 discrimination fixture. Known bound, disclosed: the test and `docs/js/decisions.js` both hand-mirror the guard's walk (bash vs Node — no byte-shared implementation possible); a parser-shape change must move the mirrors with it or the oracle drifts. The round-3-review mirror mutant (first-wins → last-wins), unpinned at review time, is pinned since APPROVED-083 by the duplicate-split fixture (b7 — measured 0 → 2 GAPs) | `pnpm dashboard-writer:test` |
| Dashboard writer self-pin prover | `scripts/probe-dashboard-writer.sh` ([GUARD-11], `decisions/APPROVED-084`, closing audit **G1** — chain step inserted after `dashboard-writer:test`) | The audit found `dashboard-writer:test` was the ONE chain link with no prover of its own, so — unlike every other gate — it was invisibly neuterable (redefine the invocation to `true` under a `package.json` ticket, or gut the test script) while the writer it guards (`docs/js/decisions.js`) is UNFROZEN by design. This prover proves the gate DISCRIMINATES and that the ways to neuter it are frozen shut, mirroring `probe-typecheck-gate.sh` (APPROVED-050/052/076): a CONTROL (the real writer passes green), a DRIFT case (reintroducing the APPROVED-058/080 double-header defect in a fixture copy of `decisions.js` — deleting the blank-gap heal in `headerBlockRange` — drives `pnpm run dashboard-writer:test` RED), a NEUTER case (the SAME broken writer flips green when the invocation is redefined to `true`, proving the freeze load-bearing), and the FREEZE at BOTH `frozen-paths.lib.sh` enumeration sites for both the test script and this prover, with `docs/js/decisions.js` as the topical negative control (the guarded writer reads NOT frozen). Every gate run goes through `pnpm run dashboard-writer:test` — the chain's own definition (APPROVED-050/[GATE-3] self-pin). **Frozen at both enumeration sites in the same change that wired it** (rule 10 pairing); its own freeze + Bash-stub + bound-grant halves are in `scripts/test-hooks.sh` (`[GUARD-11 G1 …]`) | `pnpm dashboard-writer:probe` |
| Formatter | prettier `3.9.6` (exact-pinned) with the committed `.prettierrc.json` + `.prettierignore` ([ORG-2], `decisions/APPROVED-041` — the formatter `decisions/APPROVED-017` ordered) | Formatting drift across agent-authored code stops being noise in every diff. Scope is measured, not aspirational: files dirty at wiring are root-anchored owed entries in `.prettierignore` (clean files locked clean; new files anywhere checked); `vendor/`, `.claude/skills/` (audit byte-identity), `decisions/` (prettier inserts a blank line after the H1 — the exact header-block terminator `decision_header_scan` uses, so a formatter pass voids a decision's `Authorizes:`/`Scope:`, measured) and the lockfile are policy-ignored. Fault-injection-proven in-chain; ~1.4–2s standalone. **Not style-security:** this gate proves formatting only. **Both remedies ordered by `decisions/APPROVED-044-freeze-formatter-config-and-owe-prover.md` are now IMPLEMENTED** [ORG-5] — the config pair is frozen and the gate has an in-chain prover. This row previously read "**Both remedies are now ORDERED, and neither is implemented**", measured 29/07/2026; both halves were re-measured on landing, not read off the diff. (1) **Freeze.** `.prettierrc.json` and `.prettierignore` are in `.claude/hooks/frozen-paths.lib.sh` at **both** enumeration sites — `is_protected_path` (single-path Edit/Write and Bash channels) and `repo_has_protected_paths` (repo-wide git verbs). Before: with `CLAUDE_TICKET` unset an Edit payload against either file exited **0 (allowed)**, as did `echo '**' > .prettierignore` through the Bash channel, while frozen `eslint.config.js` exited 2 as the control. After: all three exit **2**, and a decision whose `Scope:` covers the pair opens it again at exit 0 — both directions in `scripts/test-hooks.sh` `[ORG-5]`. Site 2 is proven by `PRETTIER_REPO` rather than the kernel-bearing fixture, because `repo_has_protected_paths` returns on its **first** match and short-circuits on `packages/kernel` long before reaching the new arms — measured: stripping the site-2 entries alone fails exactly the 4 repo-wide-verb cases and leaves every single-path channel green, which is what makes a one-site fix look correct. (2) **Prover.** `scripts/probe-format-gate.sh`, wired as chain step 22 `format:probe`, fault-injects both holes in throwaway copies — a mis-formatted file (red, exit 1) and a widened ignore file (which flips that same drifting tree back to green) — and asserts the clean fixture still passes, the control that stops the red cases being satisfied by an always-red step. Every gate run in it goes through **`pnpm run format:check`**, the string CI executes, per `decisions/APPROVED-050`/[GATE-3] audit H-2. Measured to discriminate before shipping: weakening `scripts.format:check` to `prettier --check . || true` drives it red, as does removing the freeze at either site alone. **The bypass it guards is unchanged and still live** — that is why the freeze, not a prettier fix, is the control: `prettier --check .` resolves `--ignore-path` to `[.gitignore, .prettierignore]` by default, so a bare `**` makes it print "All matched files use Prettier code style!" and exit 0 over a dirty tree, **byte-identical in stdout, stderr and exit code to an honest pass** (measured 30/07/2026, sha256 `17aa973d…5938f20` either way). A neutered run leaves no signal whatsoever in the CI log, which is precisely why step 21 could not be left prover-less | `pnpm format:check` + `pnpm format:probe` |

| Doc integrity (report-only) | `scripts/doc-integrity-check.sh` + its prover `scripts/probe-doc-integrity-check.sh` ([GATE-7], `decisions/APPROVED-078` Option A) | Three doc-staleness classes, mechanically, over `decisions/`, `tickets/`, `docs/` and root `*.md`: **STALE-STATUS** ("awaiting PENDING-N" / "not dispatchable until PENDING-N" prose where `decisions/APPROVED-N-*.md` already exists), **UNRESOLVED-REF** (a cited decision id, ticket id, or repo path that does not resolve on the current tree — decision ids and decision-file paths resolve by NUMBER under any status prefix, the `tickets-check.sh` rename rule, so an approval rename never floods the report; ticket-id vocabulary is derived from the `tickets/` inventory so `H-1`/`SHA-256` are never mistaken for tickets), **COUNT-DRIFT** ("N-ticket DAG" claims vs the actual `tickets/` count). **REPORT-ONLY: findings to stderr, exit ALWAYS 0** — the exit code decides nothing, by design (`APPROVED-078`'s staged posture, mirroring how memory-check's staleness half began); promotion to structural (exit 2) is a separate per-class decision, taken once that class is measured false-positive-free, and the probe asserts the exit-0 contract in BOTH directions so a drive-by promotion fails the chain. Measured at the landing branch's HEAD (03/08/2026, corrected in-review — the first-written cells undercounted their own effect): 6 stale-status, **5** count-drift (all genuine: two sites in `APPROVED-022`, `docs/README.md`, `DESIGN.md`, `ROADMAP.md` — this cell and the §14 row spell the drifted claim "56‑ticket DAG" with a NON-BREAKING hyphen, deliberately outside the checker's literal pattern, because review round 2 measured that quoting the literal makes the ledger its own finding and gives this count no stable fixed point), 687 unresolved citations over 132 unique targets (the citation count includes `docs/reference-map.md`'s baseline, which enumerates the same 132 targets — the debt is counted where it is quoted) — the unresolved class is NOT false-positive-free (deliberate example ids like `APPROVED-999`, historical/fixture paths, future-deliverable paths in open tickets), which is exactly why it ships report-only. The prover (18 cases) is fault-injection-shaped per Lesson 4: every class seeded and asserted to FIRE, every control asserted SILENT, the `--refs` resolver TSV contract pinned (MAP-1 consumes it — one scanner, not two), and the chain's own `pnpm run doc-integrity` definition exercised (`APPROVED-050` self-pin). Both scripts frozen at both `frozen-paths.lib.sh` enumeration sites (rule 10 pairing). **Stated residue:** the pair has no per-path `scripts/test-hooks.sh` site-2 discrimination fixture yet (the `TSCONFIG_REPO` shape) — `test-hooks.sh` is outside GATE-7's scope and OPS-16's mandate in the same PR is parallelization-only, so those cases are owed to the next suite-scoped ticket | `pnpm doc-integrity` + `pnpm doc-integrity:probe` |

### 2.2 Named but NOT wired — owed by SEC-4 / SEC-5 / SEC-6

Measured 27/07/2026. Until these land, any row elsewhere in this matrix
whose Proving-test cell reads "ESLint gate (§2)", "Static query audit (§2)",
"CI" or "ZAP" is citing a gate that **does not exist yet**. All such rows are
`TBD`, so no row currently claims `DONE` on a non-existent gate — but the
owning SEC ticket must land the gate before any of them may move.

> **Update 28/07/2026 (audit M3):** "SEC-4" was a phantom — no such ticket
> existed in `tickets/`, the system of record, so under AGENTS.md rule 6
> nothing could ever be dispatched for these gates. The owners are now
> real, filed `gate:CEO` on `decisions/APPROVED-030` (approved same day):
> the **Kernel boundary** and **Frontend bans** rows read as
> `tickets/SEC-4.md`; **SCA** as `tickets/SEC-5.md`; **SAST** as
> `tickets/SEC-6.md`. The query audit stays owed to the first stage that
> has queries to audit; ZAP stays Stage 5 on plan.

| Gate | Tool named | Actual state |
|---|---|---|
| **CI runner** | `.github/workflows/ci.yml` | **Present. Half-enforcing — measured 27/07/2026, not inferred from docs.** (a) A repository rule *does* block direct pushes to `main`: `git push origin main` was rejected with "push declined due to repository rule violations", so the "only reviewed merges touch main" property (DESIGN.md §9) is real and enforced by the forge, not by agent good behaviour. (b) The CI check is **not a required status check**: PR #3 merged while its `pull_request` run (#38) was still `in_progress`. So a PR can land with gates unfinished or red. The last completed run on the merged tree (#37, `2415544`) was green, but that was luck of timing, not a gate. **Updated 28/07/2026:** (b) is now half-closed. The CEO marked **`frozen-effect`** a required status check in the ruleset, established by a controlled A/B on PR #5 (failing `frozen-effect` → `blocked`; green `frozen-effect` with `gates` pending → `unstable`, which excludes both a required-review explanation and a required `gates`). **Closed 28/07/2026 — `gates` is now required as well**, so (b) is fully closed and this row's original finding no longer stands. Measured on throwaway PR #7 rather than read off the ruleset UI: one file carrying a deliberate `no-const-assign` error drove `gates` to fail at the lint step while `frozen-effect` stayed green, and the PR reported `mergeable_state: blocked`. The control is PR #5, which reported `unstable` for the same shape (`frozen-effect` green, `gates` not passing) *before* the ruleset change — and `unstable` is returned only when nothing blocking is outstanding, so a non-required failing check cannot produce `blocked`. A PR can therefore no longer land with lint, typecheck, Vitest, secretlint, the 310-case hook suite or `routing-check` red or unfinished. **Remaining owner action:** none on required checks. Note that **no pull-request review is required** on `main`: PR #5's `unstable` reading proves an unreviewed PR is mergeable once the required checks are green, so DESIGN.md §9's "only reviewed merges touch main" is enforced only in the weak sense that direct pushes are refused — the *review* half is convention. **Corrected 29/07/2026 [SEC-8]** (`decisions/APPROVED-038` Option C): that finding stands, but the remedy `[SEC-3f]` prescribed — "the CEO flips require-pull-request-review" — **cannot be executed on this repository**, and carrying it as one click away was false from the day it was written. Measured: the repo has exactly **one collaborator** (`punyanagari`, admin — `list_repository_collaborators`) and every PR is authored by that same account, because agents push and open PRs through the CEO's credential; **GitHub does not permit approving your own pull request**. Requiring ≥1 approval therefore makes *every* PR unmergeable except by admin bypass, and an admin bypass on the sole account is the advertised-control-that-does-nothing shape `APPROVED-012` rejects. What actually carries this guarantee is named in the row below |
| **Pull-request review on `main`** | Cross-model adversarial review hop (AGENTS.md / ROUTING.md), **not** a forge setting | **Replaces the `[SEC-3f]` remedy, 29/07/2026 [SEC-8]** (`decisions/APPROVED-038` Option C). The exposure is unchanged and stays visible: an unreviewed PR *can* merge once the required checks are green. What changed is the remedy, because the prescribed one is impossible here (single account, self-approval prohibited — see the CI-runner row). The control that actually carries this: **every PR touching a governed surface — `packages/kernel/**`, `.claude/**`, `docs/js/**` (§12A), the gate scripts, or any frozen path — is reviewed by an agent on a MODEL DIFFERENT from the diff's author** before merge. **Its weakness, stated rather than glossed:** this is orchestration convention enforced by AGENTS.md, **not** by the forge — a session that ignores AGENTS.md can skip it, and nothing mechanical would notice. **Its evidence, measured not asserted:** on 29/07 that hop found a CRITICAL regression (the DC-46 merge-carried frozen-path bypass, reproduced in two subclasses) **on a PR that had already merged green with every required status check passing** — which forge-required review would not have caught, since the sole human would have clicked approve on a green PR. Revisit condition: `APPROVED-038` Option B (a second/bot identity opens PRs so the CEO is always the approver) becomes available the moment a second identity exists and is worth its credential-custody cost |
| **Ticket DAG** | `scripts/tickets-check.sh` | **Wired 28/07/2026 — this row's finding is closed; the live row is in §2.1.** Ratified by `decisions/APPROVED-022` (Option A); enforces AGENTS.md rule 6 and DESIGN.md §5.2, which have asserted since bootstrap that a ticket without a `verify:` line is invalid while nothing checked it. The one-line block recorded here — wiring edits the frozen `package.json`, which `APPROVED-022`'s `Scope:` did not name — was released when the CEO added `package.json` to that decision's header `Scope:` (`274cab3`, readable only because `APPROVED-023` moved authorization parsing to the header block), and the wiring landed under `CLAUDE_TICKET=PENDING-022`. Named proving test: `scripts/probe-tickets-check.sh` — **59** cases, 0 gaps (56 at creation, +3 `gate_decision` cases in `872a1c8`) — now runs **inside the chain** as `pnpm tickets:probe`, green over the **85** tickets in `tickets/` (re-measured 29/07/2026 by running the gate against this branch's tracked tickets: `tickets-check: 85 tickets, all fields present, every verify line a command, DAG acyclic`; this cell read `46`, the [PENDING-022]-era inventory — audit M-5. The gate always covered every file in `tickets/`; only the cited count was stale). **Read `85` as a dated snapshot, not a property of the gate.** The inventory grows with every filed ticket — it moved *inside the session that corrected this cell*, as `OPS-14` was filed to close audit finding M-6 (its sibling `DC-47` was drafted and deliberately held back — `decisions/PENDING-051`) — so the durable claim is that the gate covers **every** file in `tickets/`, whatever the count; any number written here is stale the moment the next ticket lands. Run `pnpm tickets:ci` for the current figure rather than trusting this one. Honest residues: the gate proves ticket **structure**; it executes no ticket's `verify:` command, so a green chain does not mean any acceptance criterion passes — DC dispatch stays blocked until `packages/` exists — and only the front-matter block is parsed, so prose below the fence is deliberately unchecked (tickets/README.md records why) |
| SAST | Semgrep (OWASP rulesets) | **Wired 29/07/2026 [SEC-6] — this row's finding is closed; the live row is in §2.1.** Was: not installed, not configured, not in `pnpm gates`. Now: semgrep 1.172.0 pinned in CI, vendored pinned ruleset `.semgrep.yml`, `pnpm sast` + `pnpm sast:probe` in the chain, fail-closed and fault-injection-proven |
| SCA | Dependency audit on pinned lockfiles | **Wired 29/07/2026 [SEC-5] — this row's finding is closed; the live row is in §2.1.** Was: lockfile pinned but no audit step in the chain. `pnpm sca` + `pnpm sca:probe` now run in `pnpm gates`, fail-closed offline, fault-injection-proven |
| Secrets | gitleaks | Not the tool in use. The wired secrets gate is **secretlint** (§2.1); `gitleaks` happens to be on PATH but nothing invokes it. DESIGN.md §9 still cites "gitleaks gate" and is stale on this point |
| Image/IaC scan | Trivy (containers + IaC) | Not installed; no containers or IaC in the repo yet |
| Kernel boundary | ESLint import rules | **Wired 29/07/2026 [SEC-4] — this row's finding is closed; the live row is in §2.1.** Was: no such rule in `eslint.config.js`, with AGENTS.md non-negotiable 4 enforced only by the `packages/kernel` *write* freeze (a different control) and, since [DC-1], the dependency half by `packages/config/test/workspace-layout.test.ts`. The `no-restricted-imports` rule now exists, is exercised by `pnpm lint` in the chain, and is fault-injection-proven by `scripts/probe-eslint-gates.mjs` (`pnpm eslint-gates:probe`). The DC-1 test and the DC-5 source-scan remain the proof layer; lint is the fast signal. **The dependency half stopped being vacuous on 04/08/2026** — recorded because "proven by the DC-1 test" reads stronger than it was: that test's dependency cases iterate `depEntries(pkg)` over every `packages/*/package.json`, and until [DC-2] `ab8a186` every one of those files declared **zero** dependencies, so the internal-subpath and cloud-SDK dependency loops ran zero iterations (the import-scan half was always live). `packages/db` now declares `drizzle-orm` + `postgres` (runtime) and `drizzle-kit` (dev), which are the first entries those loops have ever judged. [DC-22] `5072786` also added `packages/loa` — a pure library that carries its own dependency-half proof, `packages/loa/test/corpus-manifest.test.ts` describe *"loa package purity (DC-22)"*: its manifest declares no dependency on `@auto-mb/db`/`@auto-mb/api` and its entry module imports nothing from them. Both new packages are picked up by the DC-1 test automatically (it enumerates `packages/*` on disk; its `EXPECTED_PACKAGES` list is a `toContain` floor, not an allow-list). **First cross-package CONSUMER, 05/08/2026 [DC-15] `fd41517`:** `@auto-mb/works` is the first workspace package to depend on another (`"@auto-mb/db": "workspace:*"` plus `drizzle-orm`/`postgres`), so case (e)'s internal-subpath ban finally has a real edge to judge instead of only dependency-free manifests. Every `@auto-mb/db` import in it resolves to the package ROOT — `createDbClient`/`runMigrations`/`DbConnectionOptions`, all in its test files; its `src/` imports the db package not at all and takes an injected `PostgresJsDatabase` handle instead. Nothing anywhere reaches a `@auto-mb/db/...` subpath, which is what keeps §9 row 1.2.4's parameterized-only surface intact for feature code: the `.unsafe`-carrying connection stays behind the `/migrate` subpath and this first consumer did not need it. The same rule's cost is recorded in §2.1: it is why the works package copies the db test harness rather than importing it |
| Frontend bans | ESLint (`dangerouslySetInnerHTML`, `eval`, string-built HTML) | **Wired 29/07/2026 [SEC-4] — this row's finding is closed; the live row is in §2.1.** Was: no `no-restricted-syntax` / `no-eval` rules configured. Both now exist, scoped to `packages/**`; `docs/` stays deliberately out of scope (§12A tracks its `innerHTML` inventory) and the probe asserts that exemption |
| Query audit | Static audit of every query | No such script. `scripts/` holds gate, memory and lane/gateway tooling only — `lane.sh`, `loop.sh`, `test-hooks.sh`, `memory-check.sh`, `memory-check-test.sh`, `routing-check.py`, `probe-routing-check.sh`, `gateway-contract-test.py`, `litellm-config.yaml` — none of which audits queries |
| Migration safety | Squawk (SQL AST lint) + `scripts/migration-lint.sh` (structure-only: lock/statement-timeout preamble present, no `CONCURRENTLY` inside a transaction, no bare `NOT NULL` column adds, no `drizzle-kit push` anywhere) | **Half exists now, still un-wired.** [OPS-1] landed `docs/migration-discipline.md` + `scripts/migration-lint.sh` (29/07/2026): four rules as specified, `--self-test` proves both classes by fault injection (6 fixture cases under `mktemp -d` — one discriminating plant per rule asserting the rule's own tag, not just a nonzero exit, plus two clean controls including the no-migrations-dir tolerance), stated blind spots documented in the script header (textual checker, not a SQL parser — multi-column ADD lists and plpgsql bodies; full parsing is Squawk's job). Wiring either check into the chain edits the frozen `package.json` and stays owed to a scoped decision (`decisions/APPROVED-032` "Owed wiring") — until then the lint runs only by hand or as OPS-1's verify. **The exposure stopped being hypothetical on 04/08/2026:** [DC-3] `5d45235` put the first three real hand-written migrations in `packages/db/migrations/` (`0001_uuidv7_function.sql`, `0002_create_tenants.sql`, `0003_create_audit_log.sql`), so the "no-migrations-dir tolerance" branch above no longer describes this repo. A hand run passes — `bash scripts/migration-lint.sh` printed `checked … (3 migration file(s); packages/db/migrations found)` / `OK`, exit 0 — but that is a hand measurement with no gate behind it, and the next migration to land is unlinted unless the wiring decision arrives first. **Re-measured after wave 5 (`c266605`): 13 files, still `OK`, still exit 0, still by hand** — ten more migrations (`0010`, `0011`, `0020`, `0021`, `0030`-`0032`, `0040`, `0050`, `0060`) landed unlinted-by-any-gate across three waves, which is the shape this row's "still un-wired" finding predicts and no longer merely warns about. The hand runs keep passing, which is exactly what makes the missing wiring easy to defer. Squawk itself remains absent — `tickets/DC-2.md` defers to it while nothing tracks its adoption (found by the APPROVED-032 research pass) |
| Harness portability | `scripts/portability-check.sh` + `scripts/probe-portability-check.sh` | **Wired 29/07/2026 [MIG-2] — this row's finding is closed; the live row is in §2.1.** Was: written and proven under `decisions/APPROVED-033` [MIG-1] (8-plant probe; first live run caught `LITELLM_LOG` absent from the manifest) but NOT in the chain, because wiring edits the frozen `package.json`. `decisions/APPROVED-034` (Option A) authorized the wiring and it landed under a bound session, fault-injection-proven at the `portability:ci` step. Rule 9's non-mechanical remainder stays review law, unchanged |
| DAST | OWASP ZAP baseline vs dev deployment | Not installed; scheduled Stage 5 — the one row here whose absence is on plan rather than an overclaim |

---

